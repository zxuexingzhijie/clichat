---
plan_id: 15-01
phase: 15-content-death-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - world-data/codex/locations.yaml
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "Visiting North Gate shows npc_captain and npc_hunter alongside npc_guard"
    - "Visiting Temple shows npc_herbalist alongside npc_priestess"
    - "Visiting Main Street shows npc_elder"
    - "Visiting Forest Road shows npc_hunter"
    - "Entering Dark Cave triggers enemy encounter with enemy_wolf_alpha"
  artifacts:
    - path: "world-data/codex/locations.yaml"
      provides: "Updated notable_npcs arrays and loc_dark_cave enemies field"
      contains: "npc_captain"
  key_links:
    - from: "world-data/codex/locations.yaml"
      to: "src/engine/scene-manager.ts line 117"
      via: "notable_npcs array copied to npcsPresent"
      pattern: "draft.npcsPresent = \\[...entry.notable_npcs\\]"
    - from: "world-data/codex/locations.yaml loc_dark_cave.enemies"
      to: "src/engine/action-handlers/move-handler.ts"
      via: "enemies array triggers combatLoop.startCombat"
      pattern: "location\\.enemies"
---

<objective>
Add missing NPC assignments to four locations and wire the dark cave encounter by editing locations.yaml.

Purpose: The world feels empty without the NPCs defined in npcs.yaml actually appearing in their home locations. No engine code changes needed — the scene-manager copies notable_npcs directly into npcsPresent, and move-handler already reads the enemies field.
Output: Updated world-data/codex/locations.yaml with five location entries modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@world-data/codex/locations.yaml
@world-data/codex/npcs.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing NPCs to loc_north_gate, loc_temple, loc_main_street, loc_forest_road</name>
  <files>world-data/codex/locations.yaml</files>
  <action>
Edit four locations in world-data/codex/locations.yaml. Make these exact changes and no others:

**loc_north_gate** — change notable_npcs from:
```yaml
notable_npcs:
  - npc_guard
```
to:
```yaml
notable_npcs:
  - npc_guard
  - npc_captain
  - npc_hunter
```

**loc_temple** — change notable_npcs from:
```yaml
notable_npcs:
  - npc_priestess
```
to:
```yaml
notable_npcs:
  - npc_priestess
  - npc_herbalist
```

**loc_main_street** — change notable_npcs from:
```yaml
notable_npcs: []
```
to:
```yaml
notable_npcs:
  - npc_elder
```

**loc_forest_road** — change notable_npcs from:
```yaml
notable_npcs: []
```
to:
```yaml
notable_npcs:
  - npc_hunter
```

All four NPC IDs (npc_captain, npc_hunter, npc_herbalist, npc_elder) are confirmed present in npcs.yaml. npc_hunter appears in both loc_north_gate and loc_forest_road — this is intentional per D-04 (hunter patrols between locations).
  </action>
  <verify>
    <automated>cd /Users/makoto/Downloads/work/cli && bun -e "import locs from './world-data/codex/locations.yaml'; const ng = locs.find(l => l.id === 'loc_north_gate'); const t = locs.find(l => l.id === 'loc_temple'); const ms = locs.find(l => l.id === 'loc_main_street'); const fr = locs.find(l => l.id === 'loc_forest_road'); console.assert(ng.notable_npcs.includes('npc_captain'), 'npc_captain missing from north_gate'); console.assert(ng.notable_npcs.includes('npc_hunter'), 'npc_hunter missing from north_gate'); console.assert(t.notable_npcs.includes('npc_herbalist'), 'npc_herbalist missing from temple'); console.assert(ms.notable_npcs.includes('npc_elder'), 'npc_elder missing from main_street'); console.assert(fr.notable_npcs.includes('npc_hunter'), 'npc_hunter missing from forest_road'); console.log('All NPC assertions passed');"</automated>
  </verify>
  <done>
    - loc_north_gate.notable_npcs = [npc_guard, npc_captain, npc_hunter]
    - loc_temple.notable_npcs = [npc_priestess, npc_herbalist]
    - loc_main_street.notable_npcs = [npc_elder]
    - loc_forest_road.notable_npcs = [npc_hunter]
  </done>
</task>

<task type="auto">
  <name>Task 2: Add enemy encounter to loc_dark_cave</name>
  <files>world-data/codex/locations.yaml</files>
  <action>
Edit loc_dark_cave in world-data/codex/locations.yaml. Add an enemies field after the objects array:

```yaml
- id: loc_dark_cave
  # ... all existing fields unchanged ...
  objects:
    - cave_entrance
    - strange_markings
    - bone_fragments
  enemies:
    - enemy_wolf_alpha
```

The `enemies` field in LocationSchema already exists (`z.array(z.string()).optional()` — added in Phase 12). The move-handler already reads `location.enemies` and calls `combatLoop.startCombat(enemies)` when the array is non-empty. No engine code changes are needed.

enemy_wolf_alpha (头狼, hp:35, attack:4, dc:14, danger_level:4) is confirmed present in enemies.yaml and is thematically appropriate for a danger_level:8 cave.
  </action>
  <verify>
    <automated>cd /Users/makoto/Downloads/work/cli && bun -e "import locs from './world-data/codex/locations.yaml'; const cave = locs.find(l => l.id === 'loc_dark_cave'); console.assert(Array.isArray(cave.enemies), 'enemies field missing'); console.assert(cave.enemies.includes('enemy_wolf_alpha'), 'enemy_wolf_alpha not in enemies array'); console.log('Dark cave assertion passed:', cave.enemies);"</automated>
  </verify>
  <done>loc_dark_cave has enemies: [enemy_wolf_alpha] — entering the cave will trigger combat with enemy_wolf_alpha via the existing move-handler logic</done>
</task>

</tasks>

<verification>
- `bun test` passes (no regressions from YAML edits)
- Bun native YAML import parses locations.yaml without error
- loc_north_gate shows 3 NPCs, loc_temple shows 2 NPCs, loc_main_street and loc_forest_road each show 1 NPC
- loc_dark_cave has enemies field with enemy_wolf_alpha
</verification>

<success_criteria>
All five location entries in locations.yaml contain the content specified in D-01 through D-06. bun test passes. No other locations are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-death-recovery/15-01-SUMMARY.md`
</output>
