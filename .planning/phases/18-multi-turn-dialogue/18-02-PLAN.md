---
phase: 18-multi-turn-dialogue
plan: "02"
type: tdd
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/state/dialogue-store.ts
  - src/engine/dialogue-manager.ts
  - src/engine/dialogue-manager.test.ts
  - src/ui/panels/dialogue-panel.tsx
  - src/ui/screens/game-screen.tsx
  - src/ai/prompts/npc-system.ts
  - src/ai/prompts/npc-system.test.ts
autonomous: true
requirements: [DIAL-02]
must_haves:
  truths:
    - "DialogueState.dialogueHistory 使用 {role:'user'|'assistant', content:string}[] 格式"
    - "dialogue-panel.tsx 和 game-screen.tsx 用 entry.role / entry.content 渲染历史"
    - "dialogue-manager.ts 所有历史写入点均使用新字段名"
    - "buildNpcUserPrompt 不再包含 historySection 文本序列化"
    - "DialogueManager 将 dialogueHistory 通过 conversationHistory 传给 generateNpcDialogue，实现 messages[] 多轮"
  artifacts:
    - path: "src/state/dialogue-store.ts"
      provides: "DialogueEntrySchema 迁移为 {role, content}"
      contains: "role: z.enum(['user', 'assistant'])"
    - path: "src/engine/dialogue-manager.ts"
      provides: "所有 {speaker,text} 写入点迁移为 {role,content}"
      contains: "role: 'assistant'"
    - path: "src/ai/prompts/npc-system.ts"
      provides: "historySection 删除"
      not_contains: "historySection"
  key_links:
    - from: "dialogue-manager.ts processPlayerResponse/processPlayerFreeText"
      to: "dialogueHistory array"
      via: "immer draft spread 写入 {role,content}"
      pattern: "role.*assistant.*content"
    - from: "dialogue-panel.tsx render"
      to: "DialogueEntry"
      via: "entry.role / entry.content"
      pattern: "entry\\.role"
---

<objective>
原子性迁移：将 DialogueState.dialogueHistory 的字段名从 {speaker,text} 全面迁移为 {role,content}，同时删除 npc-system.ts 中的 historySection 文本序列化。必须在单个 plan 内完成所有消费者（dialogue-store.ts + dialogue-manager.ts + dialogue-panel.tsx + game-screen.tsx）的迁移，否则 TypeScript 编译失败。

Purpose: 实现 DIAL-02 — DialogueManager 以 {role,content}[] 维护历史，并通过 messages[] 通道传给 LLM，而非文本序列化。
Output: dialogue-store.ts 新 schema；所有 4 个消费者更新；historySection 删除。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-multi-turn-dialogue/18-CONTEXT.md
@.planning/phases/18-multi-turn-dialogue/18-RESEARCH.md
@.planning/phases/18-multi-turn-dialogue/18-PATTERNS.md
@.planning/phases/18-multi-turn-dialogue/18-P01-SUMMARY.md
</context>

<interfaces>
<!-- Wave 1 产出的新接口（执行器需知道） -->

From src/ai/utils/ai-caller.ts (after Wave 1):
```typescript
// BaseCallOptions 新增可选字段：
history?: ReadonlyArray<{ readonly role: 'user' | 'assistant'; readonly content: string }>
```

From src/ai/roles/npc-actor.ts (after Wave 1):
```typescript
export type NpcActorOptions = {
  readonly maxRetries?: number;
  readonly archiveSummary?: string;
  readonly relevantCodex?: readonly string[];
  // 已迁移：
  readonly conversationHistory?: readonly { readonly role: 'user' | 'assistant'; readonly content: string }[];
};
```

From src/state/dialogue-store.ts (current — will be REPLACED in Task 1):
```typescript
const DialogueEntrySchema = z.object({
  speaker: z.enum(['npc', 'player', 'narration']),
  text: z.string(),
});
```

From src/engine/dialogue-manager.ts (current write sites — must ALL be migrated):
```typescript
// Line 411 (startDialogue):
draft.dialogueHistory = [{ speaker: 'npc', text: npcDialogue.dialogue }];
// Lines 493-497 (processPlayerResponse):
draft.dialogueHistory = [...state.dialogueHistory, { speaker: 'player', text: response.label }, { speaker: 'npc', text: npcDialogue.dialogue }];
// Lines 602-606 (processPlayerFreeText):
draft.dialogueHistory = [...state.dialogueHistory, { speaker: 'player', text }, { speaker: 'npc', text: npcDialogue.dialogue }];
// buildNpcLlmContext 参数类型 (lines 339-344)
```

From src/ui/panels/dialogue-panel.tsx (current — lines 104-107):
```typescript
<Text dimColor={entry.speaker !== 'npc'}>
  {entry.speaker === 'npc' ? `"${entry.text}"` : `你："${entry.text}"`}
```

From src/ui/screens/game-screen.tsx (current — lines 316-318):
```typescript
.filter((e) => e.speaker === 'npc')
.map((e) => `${dialogueState.npcName}："${e.text}"`)
```

From src/ai/prompts/npc-system.ts (current historySection — lines 96-101, will be DELETED):
```typescript
const historySection = context.conversationHistory?.length
  ? `\n本轮对话历史：\n${context.conversationHistory.slice(-6).map(...).join('\n')}`
  : '';
// Also: NpcUserPromptContext.conversationHistory field (to be removed)
// Also: ${historySection} in return template literal (to be removed)
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: [RED] 更新 dialogue-manager.test.ts 中所有 {speaker,text} 断言为 {role,content}</name>
  <files>src/engine/dialogue-manager.test.ts</files>
  <read_first>
    - src/engine/dialogue-manager.test.ts （全文——找到所有含 speaker / text / 'npc' / 'player' 的 dialogueHistory 断言）
    - src/state/dialogue-store.ts （当前 DialogueEntrySchema 确认）
  </read_first>
  <behavior>
    - dialogue-manager.test.ts 中所有 dialogueHistory 断言从 {speaker:'npc', text:'...'} 改为 {role:'assistant', content:'...'}
    - 所有 {speaker:'player', text:'...'} 改为 {role:'user', content:'...'}
    - 新增至少 2 个测试：startDialogue 后 dialogueHistory[0] = {role:'assistant', content}；processPlayerResponse 后 history 含 {role:'user'} 和 {role:'assistant'} 条目
    - 运行后：新断言 FAIL（源码未改），原有测试中也有 FAIL（因断言字段名变了）
  </behavior>
  <action>
  在 dialogue-manager.test.ts 中：
  1. 用 grep 定位所有含 speaker / text 的 dialogueHistory 断言行。
  2. 将这些断言改为 role / content 形式（role:'assistant' 对应原 speaker:'npc'；role:'user' 对应 speaker:'player'）。
  3. 追加 describe('dialogueHistory format — DIAL-02') 块，含：
     - "startDialogue 写入 {role:assistant, content}" 测试
     - "processPlayerResponse 追加 {role:user} + {role:assistant}" 测试
  4. 运行 `bun test src/engine/dialogue-manager.test.ts` 确认 RED 状态（含新断言的用例 FAIL）。
  </action>
  <verify>
    <automated>bun test src/engine/dialogue-manager.test.ts 2>/dev/null | grep -E "FAIL|fail" | head -5</automated>
  </verify>
  <done>测试文件已更新，运行有 FAIL（RED 状态）</done>
  <acceptance_criteria>
    - grep "speaker" src/engine/dialogue-manager.test.ts 输出 0 行（无残留旧字段）
    - grep "role.*assistant\|role.*user" src/engine/dialogue-manager.test.ts 有输出
    - bun test src/engine/dialogue-manager.test.ts 有 FAIL 输出（RED 确认）
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: [GREEN] 原子迁移所有消费者 + 删除 historySection</name>
  <files>
    src/state/dialogue-store.ts,
    src/engine/dialogue-manager.ts,
    src/ui/panels/dialogue-panel.tsx,
    src/ui/screens/game-screen.tsx,
    src/ai/prompts/npc-system.ts,
    src/ai/prompts/npc-system.test.ts
  </files>
  <read_first>
    - src/state/dialogue-store.ts （修改前完整读取）
    - src/engine/dialogue-manager.ts （修改前完整读取——特别关注 buildNpcLlmContext 函数和所有 dialogueHistory 写入点）
    - src/ui/panels/dialogue-panel.tsx （修改前读取 entry.speaker / entry.text 的所有出现位置）
    - src/ui/screens/game-screen.tsx （修改前读取 e.speaker / e.text 的所有出现位置）
    - src/ai/prompts/npc-system.ts （修改前完整读取）
    - src/ai/prompts/npc-system.test.ts （确认现有测试不依赖 historySection 或 conversationHistory 参数）
  </read_first>
  <behavior>
    【dialogue-store.ts】
    - DialogueEntrySchema: speaker→role, z.enum(['npc','player','narration'])→z.enum(['user','assistant'])，text→content

    【dialogue-manager.ts】
    - buildNpcLlmContext 参数类型：{speaker:string; text:string}[] → {role:'user'|'assistant'; content:string}[]
    - Line ~411 startDialogue: {speaker:'npc', text:...} → {role:'assistant', content:...}
    - Lines ~493-497 processPlayerResponse: {speaker:'player', text:...} → {role:'user', content:...}，{speaker:'npc', text:...} → {role:'assistant', content:...}
    - Lines ~602-606 processPlayerFreeText: 同上

    【dialogue-panel.tsx】
    - 局部 DialogueEntry 类型（如有）: speaker→role, text→content
    - entry.speaker !== 'npc' → entry.role !== 'assistant'
    - entry.speaker === 'npc' → entry.role === 'assistant'
    - entry.text → entry.content（共 2 处）

    【game-screen.tsx】
    - e.speaker === 'npc' → e.role === 'assistant'
    - e.text → e.content

    【npc-system.ts】
    - 删除 historySection const（约 lines 96-101）
    - 删除 NpcUserPromptContext 中的 conversationHistory 字段
    - 删除 return 模板字符串中的 ${historySection}

    【npc-system.test.ts】
    - 删除任何针对 conversationHistory / historySection 的测试用例（如有）
  </behavior>
  <action>
  按以下顺序修改（顺序很重要，保持 tsc 编译链不断）：

  1. dialogue-store.ts — 修改 DialogueEntrySchema（最上游 schema）。
  2. dialogue-manager.ts — 修改 buildNpcLlmContext 类型注解、3 处 dialogueHistory 写入。
  3. dialogue-panel.tsx — 修改 DialogueEntry 类型和 render 中 entry.speaker/text。
  4. game-screen.tsx — 修改 filter/map 中 e.speaker/e.text。
  5. npc-system.ts — 删除 historySection 和 NpcUserPromptContext.conversationHistory。
  6. npc-system.test.ts — 删除相关测试（若有）。

  完成后运行全量测试确认 GREEN。
  </action>
  <verify>
    <automated>bun test src/engine/dialogue-manager.test.ts src/ai/prompts/npc-system.test.ts 2>/dev/null | tail -5</automated>
  </verify>
  <done>Task 1 的所有 FAIL 用例现在 PASS；全量套件无新失败</done>
  <acceptance_criteria>
    - grep "speaker" src/state/dialogue-store.ts 输出 0 行
    - grep "speaker" src/engine/dialogue-manager.ts 输出 0 行（无残留 speaker 字段）
    - grep "entry\.speaker\|e\.speaker" src/ui/panels/dialogue-panel.tsx src/ui/screens/game-screen.tsx 输出 0 行
    - grep "historySection" src/ai/prompts/npc-system.ts 输出 0 行
    - grep "conversationHistory" src/ai/prompts/npc-system.ts 输出 0 行
    - bun test src/engine/dialogue-manager.test.ts 输出全 pass，0 fail
    - bun test 2>/dev/null | tail -3 显示 0 fail（全量回归）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialogueHistory → LLM messages[] | 内部游戏状态，由 Rules Engine 通过的 NPC 对白构成，非直接用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | Tampering | dialogueHistory content | accept | 内容来自 NPC AI 输出（已 Zod 校验的 NpcDialogue schema），玩家输入经 response.label 字段（固定选项），无自由文本注入路径 |
| T-18-02-02 | Information Disclosure | historySection 删除 | mitigate | 删除文本序列化减少提示词暴露风险；历史改走 messages[] 通道，LLM 侧不变，但玩家侧完全不可见 |
</threat_model>

<verification>
```bash
# 原子迁移验证
grep -rn "speaker" src/state/dialogue-store.ts src/engine/dialogue-manager.ts src/ui/panels/dialogue-panel.tsx src/ui/screens/game-screen.tsx
# 期望：0 行输出

grep "historySection\|conversationHistory" src/ai/prompts/npc-system.ts
# 期望：0 行输出

# 全量测试
bun test 2>/dev/null | tail -3
# 期望：0 fail
```
</verification>

<success_criteria>
- DialogueEntrySchema 使用 {role:'user'|'assistant', content:string}
- dialogue-manager.ts 的 3 处 dialogueHistory 写入全部使用 {role, content}
- dialogue-panel.tsx 和 game-screen.tsx 读取 entry.role / entry.content
- npc-system.ts 不含 historySection 或 conversationHistory
- 全量测试套件 0 fail
</success_criteria>

<output>
完成后在 .planning/phases/18-multi-turn-dialogue/ 创建 18-P02-SUMMARY.md
</output>
