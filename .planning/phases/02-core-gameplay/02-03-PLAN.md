---
phase: 02-core-gameplay
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/engine/character-creation.ts
  - src/engine/character-creation.test.ts
  - src/ui/screens/character-creation-screen.tsx
  - src/app.tsx
autonomous: false
requirements:
  - PLAY-01

must_haves:
  truths:
    - "Player can choose race from 3+ options with narrative question per D-02"
    - "Player can choose profession from 3+ options with narrative question per D-02"
    - "Player can choose background hooks that imply attribute biases per D-03"
    - "Player sees starting stats and equipment on confirm step"
    - "Character creation produces a valid PlayerState stored via playerStore"
    - "App routes to character creation screen after title screen"
  artifacts:
    - path: "src/engine/character-creation.ts"
      provides: "Character creation logic: codex queries, attribute calculation, character building"
      exports: ["createCharacterCreation"]
    - path: "src/ui/screens/character-creation-screen.tsx"
      provides: "Multi-step wizard UI with narrative questions"
      exports: ["CharacterCreationScreen"]
    - path: "src/engine/character-creation.test.ts"
      provides: "Unit tests for character creation logic"
  key_links:
    - from: "src/ui/screens/character-creation-screen.tsx"
      to: "src/engine/character-creation.ts"
      via: "createCharacterCreation() provides data and buildCharacter()"
      pattern: "createCharacterCreation"
    - from: "src/app.tsx"
      to: "src/ui/screens/character-creation-screen.tsx"
      via: "phase === 'character_creation' renders CharacterCreationScreen"
      pattern: "character_creation.*CharacterCreationScreen"
---

<objective>
Character creation wizard: narrative-driven flow where the player builds a character by answering story questions.

Purpose: PLAY-01 requires character creation with race, profession, background choices producing starting stats and equipment. Per D-01/D-02/D-03, this is a narrative experience, not a spreadsheet -- the wizard uses story questions to guide choices while attribute allocation happens implicitly.

Output: Character creation engine logic, wizard UI screen, app routing, and tests.
</objective>

<execution_context>
@.planning/phases/02-core-gameplay/02-CONTEXT.md
@.planning/phases/02-core-gameplay/02-UI-SPEC.md
@.planning/phases/02-core-gameplay/02-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-gameplay/02-01-SUMMARY.md

<interfaces>
From src/state/player-store.ts:
```typescript
export const PlayerStateSchema = z.object({
  name: z.string(),
  race: z.string(),
  profession: z.string(),
  hp: z.number(), maxHp: z.number(),
  mp: z.number(), maxMp: z.number(),
  gold: z.number(),
  attributes: z.record(AttributeNameSchema, z.number()),
  tags: z.array(z.string()),
  equipment: z.record(z.string(), z.string().nullable()),
});
```

From src/codex/schemas/entry-types.ts (after Plan 02):
```typescript
export const RaceSchema = z.object({ ...baseFields, type: z.literal("race"), traits, abilities, lore });
export const ProfessionSchema = z.object({ ...baseFields, type: z.literal("profession"), abilities, starting_equipment, primary_attribute });
export const BackgroundSchema = z.object({ ...baseFields, type: z.literal("background"), question, attribute_bias, starting_tags, world_state_effects, narrative_hook });
```

From src/state/character-creation-store.ts (from Plan 01):
```typescript
export const CharacterCreationStateSchema = z.object({
  currentStep: z.number().int().min(0).max(4),
  selectedRace: z.string().nullable(),
  selectedProfession: z.string().nullable(),
  backgroundChoices: z.array(z.string()),
  attributePreview: z.record(AttributeNameSchema, z.number()),
  isQuickMode: z.boolean(),
  isComplete: z.boolean(),
});
```

From src/codex/query.ts:
```typescript
export function queryByType(entries: Map<string, CodexEntry>, type: string): CodexEntry[];
export function queryById(entries: Map<string, CodexEntry>, id: string): CodexEntry | undefined;
```

From src/app.tsx (current routing):
```typescript
if (phase === 'title') return <TitleScreen onStart={handleStart} />;
return <GameScreen ... />;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character creation engine logic</name>
  <files>src/engine/character-creation.ts, src/engine/character-creation.test.ts</files>
  <read_first>
    src/game-loop.ts,
    src/codex/query.ts,
    src/state/player-store.ts,
    src/state/character-creation-store.ts,
    src/codex/schemas/entry-types.ts,
    src/data/codex/races.yaml,
    src/data/codex/professions.yaml,
    src/data/codex/backgrounds.yaml,
    .planning/phases/02-core-gameplay/02-CONTEXT.md
  </read_first>
  <behavior>
    - getAvailableRaces(codexEntries) returns array of Race objects with `playable` tag
    - getAvailableProfessions(codexEntries) returns array of Profession objects
    - getBackgroundHooks(codexEntries, questionKey) returns backgrounds matching that question
    - calculateAttributes(race, profession, backgrounds) returns attribute record: base {physique:2, finesse:2, mind:2} + profession primary_attribute +1 + sum of all background attribute_bias values
    - buildCharacter({name, race, profession, backgrounds}, codexEntries) returns a complete PlayerState with: name, race name, profession name, HP based on profession (adventurer:30, mage:20, rogue:25), MP (mage:12, others:8), gold 10, calculated attributes, tags merged from race traits + profession tags + background starting_tags, equipment from profession starting_equipment
    - getPresetTemplates() returns array of preset character configs (north ranger, old noble mage, wandering rogue) per D-06
  </behavior>
  <action>
Create `src/engine/character-creation.ts`:
- Factory pattern: `createCharacterCreation(codexEntries: Map<string, CodexEntry>)`
- Returns interface with: `getAvailableRaces()`, `getAvailableProfessions()`, `getBackgroundHooks(questionKey: string)`, `calculateAttributes(raceId, professionId, backgroundIds)`, `buildCharacter(selections)`, `getPresetTemplates()`
- Uses `queryByType` from `src/codex/query.ts` to find races/professions/backgrounds
- Attribute calculation per D-03/D-04: base `{physique:2, finesse:2, mind:2}` + profession primary_attribute gets +1 + sum of all selected background `attribute_bias` values
- HP/MP/Gold determined by profession:
  - adventurer: HP 30, MP 8, Gold 10
  - mage: HP 20, MP 12, Gold 8
  - rogue: HP 25, MP 8, Gold 15
  - default: HP 25, MP 8, Gold 10
- Tags: merge race.traits + profession.tags + all background.starting_tags + `['newcomer']`
- Equipment: map profession.starting_equipment strings to equipment slots (`weapon`, `armor`, `accessory`)
- Preset templates per D-06:
  - north ranger: race_human, prof_rogue, [bg_refugee, bg_secret_debt]
  - old noble mage: race_elf, prof_mage, [bg_merchant, bg_secret_noble]
  - wandering rogue: race_dwarf, prof_rogue, [bg_outlaw, bg_secret_magic]

Create `src/engine/character-creation.test.ts`:
- Test `getAvailableRaces` returns races with `playable` tag
- Test `getAvailableProfessions` returns all professions
- Test `calculateAttributes` with known inputs produces expected output
- Test `buildCharacter` with adventurer produces HP 30, expected attributes, correct equipment
- Test `buildCharacter` with mage produces HP 20, MP 12
- Test `getPresetTemplates` returns 3 templates with valid race/profession IDs
- Use actual YAML codex data loaded via codex loader (integration test)
  </action>
  <verify>
    <automated>bun test src/engine/character-creation.test.ts -v 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - src/engine/character-creation.ts exports `createCharacterCreation`
    - src/engine/character-creation.ts contains `getAvailableRaces` and `buildCharacter` and `getPresetTemplates`
    - src/engine/character-creation.test.ts has at least 6 test cases
    - All tests pass: `bun test src/engine/character-creation.test.ts` exits 0
    - buildCharacter returns a PlayerState-compatible object with correct HP (30 for adventurer, 20 for mage)
  </acceptance_criteria>
  <done>Character creation engine with codex-driven race/profession/background selection, attribute calculation, and preset templates, fully tested</done>
</task>

<task type="auto">
  <name>Task 2: Character creation wizard UI screen and app routing</name>
  <files>src/ui/screens/character-creation-screen.tsx, src/app.tsx</files>
  <read_first>
    src/ui/screens/title-screen.tsx,
    src/ui/screens/game-screen.tsx,
    src/ui/panels/actions-panel.tsx,
    src/app.tsx,
    src/state/game-store.ts,
    src/state/character-creation-store.ts,
    .planning/phases/02-core-gameplay/02-UI-SPEC.md
  </read_first>
  <action>
**Create `src/ui/screens/character-creation-screen.tsx`:**
Follow title-screen.tsx pattern (props with callback) combined with game-screen.tsx pattern (complex state):

Props: `readonly onComplete: (playerState: PlayerState) => void`

Internal state managed via characterCreationStore:
- Step 0 (Race): Display "你从哪里来？" as bold text, list races using actions-panel style (numbered list with `>` cursor, up/down navigation, Enter to select, number keys for quick-select)
- Step 1 (Profession): Display "你靠什么活下去？" as bold text, list professions
- Step 2 (Background): Display "你为什么来到黑松镇？" as bold text, list background hooks for this question, then "你身上有什么秘密？" for second background selection
- Step 3 (Confirm): Display character summary — name, race, profession, background descriptions, attribute preview, starting equipment, "开始冒险" as primary CTA

Layout per UI-SPEC:
```
┌─ Chronicle CLI ─────────────────────── 角色创建 ─┐
│                                                   │
│  {narrative question in bold}                     │
│                                                   │
│  > 1. {option name} — {option description}        │
│    2. {option name} — {option description}        │
│    3. {option name} — {option description}        │
│                                                   │
│  ↑↓ 选择    Enter 确认                             │
│                                                   │
├─────────────────────────────────────────────────────┤
│  步骤 {n}/4  ■□□□                                  │
└─────────────────────────────────────────────────────┘
```

- Use `useInput` from ink for keyboard handling (same pattern as actions-panel.tsx)
- Use `useScreenSize` from fullscreen-ink for responsive layout
- Progress bar at bottom: `步骤 {n}/4` with `■` for completed steps and `□` for remaining
- Escape goes to previous step (step 0 escape does nothing or returns to title)
- On confirm: call `createCharacterCreation(codexEntries).buildCharacter(selections)` to get PlayerState, then call `onComplete(playerState)`
- Load codex entries using `loadAllEntries()` from `src/codex/loader.ts`

**Update `src/app.tsx`:**
- Import `CharacterCreationScreen` from `./ui/screens/character-creation-screen`
- Import `characterCreationStore` from `./state/character-creation-store`
- Change `handleStart` to set `phase: 'character_creation'` instead of `'game'`
- Add new handler `handleCharacterCreated(playerState)`:
  - Updates playerStore with the new character state via `playerStore.setState(draft => { Object.assign(draft, playerState); })`
  - Sets `gameStore` phase to `'game'`
- In AppInner routing:
  - `if (phase === 'title') return <TitleScreen onStart={handleStart} />;`
  - `if (phase === 'character_creation') return <SizeGuard><CharacterCreationScreen onComplete={handleCharacterCreated} /></SizeGuard>;`
  - default: `return <SizeGuard><GameScreen ... /></SizeGuard>;`
  </action>
  <verify>
    <automated>bun build src/app.tsx --no-bundle --target=bun 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/screens/character-creation-screen.tsx exports `CharacterCreationScreen`
    - src/ui/screens/character-creation-screen.tsx contains "你从哪里来？" and "你靠什么活下去？" and "步骤"
    - src/app.tsx contains `'character_creation'` in the phase routing
    - src/app.tsx contains `CharacterCreationScreen` import
    - src/app.tsx contains `handleCharacterCreated` callback
    - Build succeeds without type errors
  </acceptance_criteria>
  <done>Character creation wizard renders 4 steps with narrative questions, progress bar, keyboard navigation; app routes title -> character creation -> game</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify character creation wizard visually</name>
  <action>Human verifies the character creation wizard renders correctly and interactions work as designed.</action>
  <what-built>Character creation wizard — a narrative-driven flow where the player creates their character through story questions.</what-built>
  <how-to-verify>
    1. Run `bun run src/index.tsx` (or the main entry point)
    2. Press any key on title screen to proceed
    3. You should now see the character creation wizard with "你从哪里来？" and race options
    4. Use arrow keys to navigate, Enter to select a race
    5. Step 2: profession selection with "你靠什么活下去？"
    6. Step 3: background selection with "你为什么来到黑松镇？" and "你身上有什么秘密？"
    7. Step 4: character summary with stats, equipment, and "开始冒险" button
    8. Press Enter on confirm to enter the game screen
    9. Verify: status bar shows the stats you selected, not defaults
    10. Press Escape on step 2 to verify back-navigation works
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/interaction issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codex YAML -> character creation | Player selections come from codex data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | Character attribute calculation | mitigate | Attributes calculated by engine from codex data, not from user input. User selects IDs only; engine resolves to validated codex entries. |
| T-02-06 | Elevation of Privilege | buildCharacter function | mitigate | Zod schema validation on PlayerState output ensures no invalid attribute values. All attribute bonuses come from schema-validated codex entries. |
</threat_model>

<verification>
- `bun test src/engine/character-creation.test.ts` passes
- `bun build src/app.tsx --no-bundle` compiles
- Character creation wizard visually matches UI-SPEC layout
</verification>

<success_criteria>
Player can create a character through 4-step narrative wizard, see starting stats/equipment, and enter the game with their created character. Engine logic fully tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-gameplay/02-03-SUMMARY.md`
</output>
