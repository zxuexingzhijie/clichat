---
phase: 23-world-pack-platform
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - src/world-packs/validate.ts
  - src/world-packs/validate.test.ts
autonomous: true
requirements:
  - WPK-01
  - WPK-02
  - WPK-03
  - WPK-04
must_haves:
  truths:
    - "Validation catches malformed YAML, broken refs, missing dependencies, cycles, duplicate IDs, and namespace rule violations before runtime."
    - "Author-facing validation output is deterministic and actionable."
    - "Validation reuses loader/schema contracts rather than inventing a second schema system."
  artifacts:
    - path: "src/world-packs/validate.ts"
      provides: "Deep validation service for authoring CLI and Classic Fantasy self-validation"
      exports: ["validateWorldPacks", "formatValidationResult"]
  key_links:
    - from: "src/world-packs/validate.ts"
      to: "src/world-packs/loader.ts"
      via: "loadWorldPacks/toposort validation"
      pattern: "loadWorldPacks|topologicallySortPacks"
    - from: "src/world-packs/validate.ts"
      to: "src/world-packs/namespace.ts"
      via: "namespace rule checks for refs"
      pattern: "isNamespacedId|normalize"
---

<objective>
Add deep world-pack validation as a focused service.

Purpose: Authors and runtime boot need a validator that catches schema, reference, dependency, and namespace failures before invalid packs reach gameplay.
Output: `validateWorldPacks` service, deterministic formatter, and focused validation tests.
</objective>


<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-02-SUMMARY.md
@src/world-packs/loader.ts
@src/codex/schemas/entry-types.ts
@src/codex/schemas/relationship.ts


<interfaces>
From Plan 23-02 expected exports:
```typescript
export async function loadWorldPacks(options: LoadWorldPacksOptions): Promise Promise<WorldState>;
export function topologicallySortPacks(manifests: LoadedPackManifest[]): LoadedPackManifest[];
export async function loadPackManifest(packDir: string): Promise Promise<LoadedPackManifest>;
```

Validation result contract to implement:
```typescript
export interface ValidationResult {
  ok: boolean;
  errors: string[];
  warnings: string[];
  worldState?: WorldState;
}
export async function validateWorldPacks(options: ValidateWorldPacksOptions): Promise Promise<ValidationResult>;
export function formatValidationResult(result: ValidationResult): string[];
```
</interfaces>
</context>


<tasks>


<task type="auto" tdd="true">
  <name>Task 1: Implement validation result contract and dependency/schema checks</name>
  <files>src/world-packs/validate.ts, src/world-packs/validate.test.ts</files>
  <behavior>
    - Valid temp packs return `{ ok: true, errors: [] }` with a `worldState`.
    - Malformed manifest/YAML/schema failures return `{ ok: false }` with deterministic file/path details.
    - Missing dependencies and cycles are reported deterministically per D-06/D-10.
    - Duplicate fully-qualified IDs are surfaced as validation errors per D-05.
  </behavior>
  <action>Create `src/world-packs/validate.ts` returning `{ ok, errors, warnings, worldState? }` and `formatValidationResult`. Wrap loader/toposort/schema errors into deterministic author-facing validation messages per D-10/D-12. Reuse `WorldPackManifestSchema` and `loadWorldPacks`; do not build a parallel schema system. Do not restructure Classic Fantasy data in this plan.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/validate.test.ts src/world-packs/loader.test.ts</automated>
  </verify>
  <done>Validation catches manifest/schema/dependency/duplicate failures using loader contracts, with deterministic result formatting for CLI consumers.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 2: Add reference integrity and namespace validation</name>
  <files>src/world-packs/validate.ts, src/world-packs/validate.test.ts</files>
  <behavior>
    - Broken same-pack refs fail with file/entity/path details.
    - Bare refs that point outside the same pack fail; no cross-pack guessing per D-07.
    - Explicit full cross-pack refs pass only if a required dependency provides the entity.
    - Validation output remains stable sorted by file/entity/path.
  </behavior>
  <action>Extend validation to traverse structured codex reference fields after loader normalization. Verify every normalized same-pack reference resolves within the merged WorldState; verify explicit cross-pack refs resolve and the providing pack is declared as a required dependency. Preserve D-04 authoring ergonomics for same-pack bare refs while enforcing D-07 explicit cross-pack refs. Include relationship refs and quest/objective refs covered by `entry-types.ts`; do not prefix prose or tags.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/validate.test.ts src/world-packs/namespace.test.ts</automated>
  </verify>
  <done>`validateWorldPacks` performs D-10 deep validation for refs/deps/namespace rules and is ready for `chronicle validate` and Classic Fantasy self-validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pack YAML → validator | Author content is parsed and judged before runtime loading. |
| validation result → author decisions | CLI output guides authors in fixing pack content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Spoofing | cross-pack refs | mitigate | Require explicit `@pack/entity_id` and dependency declaration for cross-pack refs. |
| T-23-03-02 | Repudiation | validation errors | mitigate | Include deterministic file/entity/path details in validation messages. |
| T-23-03-03 | Denial of Service | reference traversal | mitigate | Traverse known structured fields and sorted maps; avoid unbounded arbitrary prose scanning. |
</threat_model>


<verification>
Run `/Users/makoto/.bun/bin/bun test src/world-packs/validate.test.ts src/world-packs/loader.test.ts src/world-packs/namespace.test.ts`.
</verification>


<success_criteria>
- `validateWorldPacks` catches broken refs/deps/namespace problems before runtime.
- D-05, D-06, D-07, D-10, and D-12 are represented in focused tests.
- CLI authoring plan can consume the validation service directly.
</success_criteria>


<output>
After completion, create `.planning/phases/23-world-pack-platform/23-03-SUMMARY.md`.
</output>
