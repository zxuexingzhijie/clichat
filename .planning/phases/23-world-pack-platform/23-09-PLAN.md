---
phase: 23-world-pack-platform
plan: 09
type: execute
wave: 9
depends_on:
  - 23-08
files_modified:
  - src/persistence/save-migrator.ts
  - src/persistence/save-migrator.test.ts
  - src/state/serializer.ts
  - src/state/serializer.test.ts
autonomous: true
requirements:
  - WPK-07
must_haves:
  truths:
    - "Existing v1.4/latest pre-Phase-23 saves migrate eagerly to a latest schema with @classic_fantasy entity refs."
    - "Migration is idempotent and does not double-prefix already namespaced refs."
    - "Serializer writes the latest namespaced save format after restore/snapshot."
  artifacts:
    - path: "src/persistence/save-migrator.ts"
      provides: "V7→V8 or equivalent eager namespace migration"
      exports: ["migrateToLatest"]
    - path: "src/state/serializer.ts"
      provides: "Latest SaveData schema and snapshot/restore using namespaced refs"
    - path: "src/persistence/save-migrator.test.ts"
      provides: "Migration coverage for nested saved entity references"
    - path: "src/state/serializer.test.ts"
      provides: "Serializer latest-version and idempotency coverage"
  key_links:
    - from: "src/state/serializer.ts"
      to: "src/persistence/save-migrator.ts"
      via: "restore parses migrated latest schema"
      pattern: "migrateToLatest"
    - from: "src/persistence/save-migrator.ts"
      to: "src/world-packs/namespace.ts"
      via: "reuse namespace parser/formatter for idempotent prefixing"
      pattern: "toNamespacedId|isNamespacedId"
---

<objective>
Add the eager namespaced save migration and update serializer schema/tests.

Purpose: Pack composition is unsafe if persisted saves keep bare Classic Fantasy IDs; migration must convert old saves to the locked `@classic_fantasy/entity_id` format without data loss.
Output: Latest save schema/migration, serializer updates, and focused migration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-08-SUMMARY.md
@src/persistence/save-migrator.ts
@src/state/serializer.ts
@src/state/scene-store.ts
@src/state/quest-store.ts
@src/state/world-memory-store.ts

<interfaces>
Existing/latest pattern from research:
```typescript
export function migrateToLatest(raw: unknown): SaveData;
export const SaveDataV7Schema = ...;
```

Expected Phase 23 direction:
```typescript
export const SaveDataV8Schema = SaveDataV7Schema.extend({ version: z.literal(8) });
export function migrateV7ToV8(raw: unknown): unknown;
```
Use V8 unless code inspection during execution proves a deliberate V7 augmentation is safer; D-03 requires eager persisted namespacing either way.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add latest save schema and eager namespace migration</name>
  <files>src/persistence/save-migrator.ts, src/persistence/save-migrator.test.ts, src/state/serializer.ts, src/state/serializer.test.ts</files>
  <behavior>
    - A valid pre-Phase-23 save migrates to latest version with `@classic_fantasy/...` refs per D-03.
    - Migration covers scene ID, current location, quest refs, NPC/location/item refs, relations, event logs, world memory, branch refs, and player/world memory containers identified by current serializer state.
    - Already namespaced refs remain unchanged; migration is idempotent.
    - Non-entity prose, tags, generated event IDs, and arbitrary text are not prefixed.
  </behavior>
  <action>Add a latest save schema (likely `SaveDataV8Schema` because research found current latest is V7) and a chained migration from V7 to latest. Centralize field-aware namespace conversion using helpers from `src/world-packs/namespace.ts` where practical. Convert all persisted/runtime entity references named in D-01, not just `scene.sceneId`. Keep the migration eager on load/restore and ensure serializer snapshots write the latest version thereafter.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/persistence/save-migrator.test.ts src/state/serializer.test.ts src/world-packs/namespace.test.ts</automated>
  </verify>
  <done>Old saves migrate eagerly and idempotently to a latest namespaced format, and serializer tests prove no double-prefix or prose corruption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify serializer restore/snapshot writes namespaced latest saves</name>
  <files>src/state/serializer.ts, src/state/serializer.test.ts</files>
  <behavior>
    - `restore` calls `migrateToLatest` before parsing latest schema.
    - `snapshot` emits the latest version number and namespaced IDs for current runtime state.
    - Save/load round trip preserves namespaced IDs in scene, quest, relation, event log, world memory, and branch-related state.
  </behavior>
  <action>Update serializer restore/snapshot code and tests so migrated state becomes the canonical in-memory/latest persisted form. If runtime stores still expose bare Classic Fantasy defaults during test construction, update test fixtures to use `@classic_fantasy/...` values rather than weakening the migration. Do not mutate old save files in place outside the existing save/load flow.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/state/serializer.test.ts src/persistence/save-migrator.test.ts</automated>
  </verify>
  <done>Serializer round trips latest namespaced save data and WPK-07 is covered by focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| old save JSON → latest runtime state | User/local persisted data is parsed, migrated, and trusted by game systems. |
| migration traversal → user prose | Field-aware conversion must not alter non-entity strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-09-01 | Tampering | `migrateV7ToV8`/latest migration | mitigate | Parse through versioned schema chain and test nested refs; never mutate unsupported structures blindly. |
| T-23-09-02 | Information Disclosure | migration errors | accept | Errors expose local save field paths only, useful for recovery and not network-visible. |
| T-23-09-03 | Denial of Service | recursive migration | mitigate | Traverse known save schema fields and containers, not arbitrary deep user strings. |
</threat_model>

<verification>
Run `/Users/makoto/.bun/bin/bun test src/persistence/save-migrator.test.ts src/state/serializer.test.ts src/world-packs/namespace.test.ts`.
</verification>

<success_criteria>
- Latest save schema/migration eagerly converts bare Classic Fantasy refs to `@classic_fantasy/...`.
- Migration covers all D-01 persisted reference categories present in current serializer state.
- Serializer writes and restores the latest namespaced format without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/23-world-pack-platform/23-09-SUMMARY.md`.
</output>
