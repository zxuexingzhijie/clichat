---
phase: 23-world-pack-platform
plan: 08
type: execute
wave: 8
depends_on:
  - 23-07
files_modified:
  - src/cli.ts
  - src/cli.test.ts
  - src/index.tsx
  - src/app.tsx
  - src/app.test.tsx
  - src/paths.ts
  - src/paths.test.ts
autonomous: true
requirements:
  - WPK-05
  - WPK-06
  - WPK-08
must_haves:
  truths:
    - "WorldPackLoader/cache runs from the CLI pre-React boundary before default game boot."
    - "App consumes a prepared WorldState/codex map instead of loading `world-data/codex` in a React effect."
    - "Pack-aware path resolution preserves existing data-dir compatibility while removing runtime hardcoding to `world-data/codex`."
  artifacts:
    - path: "src/cli.ts"
      provides: "Default boot path that loads cached WorldState before importing React"
    - path: "src/index.tsx"
      provides: "Render boundary that passes prepared WorldState into App or exposes a boot-state handoff"
    - path: "src/app.tsx"
      provides: "App consumption of prepared codex entries without direct world codex file I/O"
    - path: "src/paths.ts"
      provides: "Pack-aware path helpers used by boot and tests"
  key_links:
    - from: "src/cli.ts"
      to: "src/world-packs/cache.ts"
      via: "loadWorldStateWithCache before React import"
      pattern: "loadWorldStateWithCache"
    - from: "src/index.tsx"
      to: "src/app.tsx"
      via: "WorldState/App prop or boot-state module"
      pattern: "worldState|initialCodexEntries|bootState"
    - from: "src/app.tsx"
      to: "src/codex/loader.ts"
      via: "removed runtime loadAllCodex effect"
      pattern: "loadAllCodex"
---

<objective>
Wire cached WorldState into the default pre-React boot path and App consumption boundary.

Purpose: The platform requirement is not complete until normal gameplay starts from a prepared pack-loaded WorldState instead of React reading `world-data/codex` directly.
Output: CLI boot integration, App/index handoff, path resolution updates, and focused tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-07-SUMMARY.md
@src/cli.ts
@src/index.tsx
@src/app.tsx
@src/paths.ts
@src/world-packs/cache.ts

<interfaces>
From Plan 23-07 expected exports:
```typescript
export async function loadWorldStateWithCache(options: { packDirs: string[]; noCache?: boolean }): Promise<WorldState>;
```

From Plan 23-02 expected type:
```typescript
export interface WorldState {
  readonly entries: ReadonlyMap<string, CodexEntry>;
  readonly packOrder: readonly string[];
  readonly sourceFiles: readonly string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Call cached WorldPackLoader before React import in CLI boot</name>
  <files>src/cli.ts, src/cli.test.ts, src/paths.ts, src/paths.test.ts</files>
  <behavior>
    - Default no-subcommand boot resolves root `world-data/manifest.yaml`/pack dirs and calls `loadWorldStateWithCache` before importing `./index` per D-13.
    - `--no-cache` bypasses cache reads/writes if wired, or equivalent rebuild path exists per D-18.
    - `--world-dir` and `CHRONICLE_WORLD_DIR` compatibility remains deterministic.
    - Authoring subcommands from Plan 23-06 still do not start React.
  </behavior>
  <action>Update the Commander default `.action` in `src/cli.ts` so normal game boot resolves pack directories via `src/paths.ts`, calls `loadWorldStateWithCache`, and only then imports the React render boundary. Pass the prepared WorldState through a minimal boot-state module, explicit setter, or props-compatible handoff chosen consistently with existing `index.tsx`. Preserve authoring subcommands and existing `--world-dir` semantics. Do not move loader logic into React, and do not reintroduce `world-data/codex` as a runtime source.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/cli.test.ts src/paths.test.ts src/world-packs/cache.test.ts</automated>
  </verify>
  <done>Tests prove the default CLI path calls cached pack loading before React import, while authoring commands stay pre-React and deterministic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make App consume prepared WorldState and remove React-side codex loading</name>
  <files>src/index.tsx, src/app.tsx, src/app.test.tsx</files>
  <behavior>
    - App receives prepared `WorldState.entries`/codex map and initializes existing codex-dependent managers from it.
    - App no longer calls `loadAllCodex` inside `useEffect` for runtime boot per D-14.
    - Existing derived `codexDisplayEntries`, quest templates, map data, and provider wiring still receive the same entry shapes.
    - Missing prepared state fails with an actionable boot error rather than silently reading legacy codex.
  </behavior>
  <action>Update `src/index.tsx` and `src/app.tsx` so App consumes the prepared WorldState/codex map created by CLI boot. Preserve existing React/Ink provider boundaries and manager construction; only replace the data source. Add or update `src/app.test.tsx` to assert no direct `loadAllCodex` runtime effect remains and that App can initialize from an injected prepared WorldState. Do not add a second loader path in React.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/app.test.tsx src/cli.test.ts</automated>
  </verify>
  <done>App uses prepared pack-loaded WorldState, and WPK-05/WPK-08 are enforced by source/integration tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args/env → pack path resolution | User-controlled paths select pack directories. |
| cached WorldState → React App | Prepared engine data crosses into UI runtime. |
| boot handoff → global/module state | If using a boot-state module, stale or missing state can affect startup correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08-01 | Tampering | CLI path resolution | mitigate | Use path helpers with resolved directories and tests for `--world-dir`/env behavior. |
| T-23-08-02 | Repudiation | boot failures | mitigate | Missing/invalid prepared state fails with actionable error before App silently falls back. |
| T-23-08-03 | Denial of Service | duplicate load paths | mitigate | Remove React-side `loadAllCodex` boot effect so only one loader/cache path feeds App. |
</threat_model>

<verification>
Run `/Users/makoto/.bun/bin/bun test src/cli.test.ts src/app.test.tsx src/paths.test.ts src/world-packs/cache.test.ts` and grep the runtime App path to confirm it no longer imports/calls `loadAllCodex` for boot.
</verification>

<success_criteria>
- WorldPackLoader/cache is called before React startup from `src/cli.ts`.
- `src/app.tsx` consumes prepared WorldState/codex entries and does not load `world-data/codex` in a React effect.
- D-13, D-14, D-18, WPK-05, WPK-06, and WPK-08 are fully implemented without changing Phase 24 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/23-world-pack-platform/23-08-SUMMARY.md`.
</output>
