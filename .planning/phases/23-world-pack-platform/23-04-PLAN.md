---
phase: 23-world-pack-platform
plan: 04
type: execute
wave: 4
depends_on:
  - 23-03
files_modified:
  - world-data/manifest.yaml
  - world-data/packs/classic_fantasy/manifest.yaml
  - world-data/packs/classic_fantasy/codex/races.yaml
  - world-data/packs/classic_fantasy/codex/professions.yaml
  - world-data/packs/classic_fantasy/codex/backgrounds.yaml
  - world-data/packs/classic_fantasy/codex/locations.yaml
  - world-data/packs/classic_fantasy/codex/items.yaml
  - world-data/packs/classic_fantasy/codex/factions.yaml
autonomous: true
requirements:
  - WPK-01
  - WPK-02
  - WPK-03
must_haves:
  truths:
    - "Classic Fantasy has root and pack manifests that identify the built-in pack as @classic_fantasy."
    - "Core Classic Fantasy codex groups live under the pack directory and validate through the deep validator."
    - "No optional dependencies, registry metadata, template variants, or last-wins override semantics are introduced."
  artifacts:
    - path: "world-data/manifest.yaml"
      provides: "Root built-in pack set manifest pointing at packs/classic_fantasy"
    - path: "world-data/packs/classic_fantasy/manifest.yaml"
      provides: "Built-in Classic Fantasy pack manifest with id classic_fantasy"
    - path: "world-data/packs/classic_fantasy/codex/{races,professions,backgrounds,locations,items,factions}.yaml"
      provides: "Core Classic Fantasy codex content in pack layout"
  key_links:
    - from: "world-data/manifest.yaml"
      to: "world-data/packs/classic_fantasy/manifest.yaml"
      via: "root pack list path"
      pattern: "packs/classic_fantasy"
    - from: "world-data/packs/classic_fantasy/manifest.yaml"
      to: "src/world-packs/validate.ts"
      via: "validateWorldPacks reads manifest schema"
      pattern: "id: classic_fantasy"
---

<objective>
Create the Classic Fantasy pack manifests and move the core codex groups into the built-in pack layout.

Purpose: Classic Fantasy must become the canonical `@classic_fantasy` pack before runtime boot can load it through the same path as author packs.
Output: Root pack-set manifest, Classic Fantasy pack manifest, and core codex YAML under `world-data/packs/classic_fantasy/codex/`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-03-SUMMARY.md
@world-data/world-manifest.json
@world-data/codex/locations.yaml
@src/world-packs/validate.ts

<interfaces>
From Plan 23-03 expected exports:
```typescript
export async function validateWorldPacks(options: ValidateWorldPacksOptions): Promise<ValidationResult>;
export function formatValidationResult(result: ValidationResult): string[];
```

Locked pack ID per D-02:
```typescript
export const CLASSIC_FANTASY_PACK_ID = "classic_fantasy";
export const CLASSIC_FANTASY_PREFIX = "@classic_fantasy";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Classic Fantasy pack-set and pack manifests</name>
  <files>world-data/manifest.yaml, world-data/packs/classic_fantasy/manifest.yaml</files>
  <action>Create `world-data/packs/classic_fantasy/manifest.yaml` with `id: classic_fantasy`, valid display metadata, `schemaVersion: "1.0.0"`, `directories.codex: codex`, and `dependencies.required: []` per D-02/D-06. Create `world-data/manifest.yaml` as the root built-in pack-set manifest pointing at `packs/classic_fantasy`. Do not use `@classic`, `@core`, optional dependencies, registry fields, marketplace fields, or template variants.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/manifest-schema.test.ts src/world-packs/validate.test.ts</automated>
  </verify>
  <done>Root and pack manifests validate under WPK-01 and identify the built-in pack as `classic_fantasy` only.</done>
</task>

<task type="auto">
  <name>Task 2: Move core Classic Fantasy codex files into pack layout</name>
  <files>world-data/packs/classic_fantasy/codex/races.yaml, world-data/packs/classic_fantasy/codex/professions.yaml, world-data/packs/classic_fantasy/codex/backgrounds.yaml, world-data/packs/classic_fantasy/codex/locations.yaml, world-data/packs/classic_fantasy/codex/items.yaml, world-data/packs/classic_fantasy/codex/factions.yaml</files>
  <action>Move or copy the existing core codex groups from `world-data/codex/` into `world-data/packs/classic_fantasy/codex/` using the existing file grouping. Preserve `world-data/ai-config.yaml`, `world-data/narrative-transitions.yaml`, `world-data/memory/`, and other non-codex runtime/config files outside the pack. Fix validation failures only by applying D-01/D-04 namespace rules: same-pack authoring refs may remain bare for loader normalization; persisted/runtime references must be full `@classic_fantasy/...`.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/validate.test.ts src/world-packs/loader.test.ts</automated>
  </verify>
  <done>Core Classic Fantasy codex files exist in the pack directory and validate without introducing any deferred override or dependency behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built-in YAML → runtime WorldState | First-party content still crosses schema/validation boundaries before gameplay. |
| legacy layout → pack layout | Moving data can accidentally duplicate or orphan runtime sources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Spoofing | Classic Fantasy pack ID | mitigate | Manifest uses exact `classic_fantasy` ID and validation proves `@classic_fantasy` output. |
| T-23-04-02 | Tampering | content restructure | mitigate | Deep validator runs after restructure and catches broken refs/schema errors. |
| T-23-04-03 | Repudiation | legacy/new source ambiguity | mitigate | Runtime plans must consume the pack path and not add a new dependency on legacy `world-data/codex`. |
</threat_model>

<verification>
Run `/Users/makoto/.bun/bin/bun test src/world-packs/validate.test.ts src/world-packs/loader.test.ts src/world-packs/manifest-schema.test.ts`.
</verification>

<success_criteria>
- Classic Fantasy root and pack manifests exist and validate.
- Core codex groups are available under `world-data/packs/classic_fantasy/codex/`.
- D-01, D-02, D-04, D-06, and D-10 are preserved without optional deps, registry, or last-wins behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/23-world-pack-platform/23-04-SUMMARY.md`.
</output>
