---
phase: 23-world-pack-platform
plan: 07
type: execute
wave: 7
depends_on:
  - 23-06
files_modified:
  - src/world-packs/cache.ts
  - src/world-packs/cache.test.ts
  - src/paths.ts
  - src/paths.test.ts
autonomous: true
requirements:
  - WPK-06
  - WPK-08
must_haves:
  truths:
    - "Warm cache stores merged WorldState JSON in the user cache dir."
    - "Recursive mtime+size hash invalidates cache when any loaded pack file changes."
    - "Cache artifacts are never written into pack source directories or `world-data/`."
  artifacts:
    - path: "src/world-packs/cache.ts"
      provides: "env-paths cache, recursive mtime+size hash, JSON WorldState serialization"
      exports: ["loadWorldStateWithCache", "computePackSetHash", "getWorldPackCacheDir"]
    - path: "src/paths.ts"
      provides: "Pack-aware world-data and cache path helpers"
  key_links:
    - from: "src/world-packs/cache.ts"
      to: "src/world-packs/loader.ts"
      via: "loadWorldPacks cache miss/rebuild"
      pattern: "loadWorldPacks"
    - from: "src/world-packs/cache.ts"
      to: "env-paths"
      via: "getWorldPackCacheDir"
      pattern: "envPaths"
---

<objective>
Implement cached WorldState loading and pack-aware path helpers.

Purpose: Pre-React boot needs a fast, valid cache boundary before App integration can consume prepared WorldState.
Output: cache module, recursive invalidation, env-paths cache location, path helper updates, and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-06-SUMMARY.md
@src/paths.ts
@src/persistence/save-file-manager.ts
@src/world-packs/loader.ts

<interfaces>
From Plan 23-02 expected exports:
```typescript
export async function loadWorldPacks(options: LoadWorldPacksOptions): Promise<WorldState>;
export interface WorldState {
  readonly entries: ReadonlyMap<string, CodexEntry>;
  readonly packOrder: readonly string[];
  readonly sourceFiles: readonly string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement recursive pack-set hashing and env-paths cache location</name>
  <files>src/world-packs/cache.ts, src/world-packs/cache.test.ts, src/paths.ts, src/paths.test.ts</files>
  <behavior>
    - Cache directory resolves under `envPaths('Chronicle', { suffix: '' }).cache` per D-17.
    - Recursive hash includes relativePath, size, and mtimeMs for all loaded pack files per D-16.
    - Editing, adding, deleting, or renaming any nested pack file changes the hash.
    - Cache files are never created under pack dirs or `world-data`.
  </behavior>
  <action>Create `src/world-packs/cache.ts` with `getWorldPackCacheDir` and `computePackSetHash(packDirs)`. Use `env-paths` already in dependencies; add path helpers in `src/paths.ts` only where they simplify resolving root `world-data/manifest.yaml` and pack directories. Sort recursive stat metadata deterministically. The hash is for invalidation, not security. Tests must use temp dirs and verify cache path isolation from source directories.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/cache.test.ts src/paths.test.ts</automated>
  </verify>
  <done>Cache location and invalidation hash satisfy D-16/D-17 and WPK-06 without CLI boot/App changes in this plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize cached WorldState and rebuild on invalidation</name>
  <files>src/world-packs/cache.ts, src/world-packs/cache.test.ts</files>
  <behavior>
    - Cache metadata includes pack set, cache version, source file list, and recursive hash per D-15/D-16.
    - Cache hit deserializes WorldState JSON back into `Map<string, CodexEntry>`.
    - Cache miss or stale metadata calls `loadWorldPacks` and rewrites cache.
    - Warm cache function path returns in <5ms in a focused test excluding Bun startup and React render.
    - `noCache` bypasses reads/writes if implemented, satisfying D-18 without making it required for correctness.
  </behavior>
  <action>Implement `serializeWorldState`, `deserializeWorldState`, and `loadWorldStateWithCache({ packDirs, noCache? })`. Store JSON-safe entries as arrays and reconstruct `Map<string, CodexEntry>`. Validate cache metadata before use; fail closed to rebuild on mismatch/corruption. Include `--no-cache`-ready option in API for later CLI boot integration if practical. Do not import React or App modules.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/cache.test.ts src/world-packs/loader.test.ts</automated>
  </verify>
  <done>WorldState cache satisfies D-15/D-16/D-17 with recursive invalidation, JSON serialization, rebuild behavior, and focused warm-cache <5ms measurement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pack files → cache file | Local pack data is serialized to reusable JSON. |
| cache file → runtime WorldState | Cache is trusted only when metadata matches. |
| filesystem metadata → invalidation | Recursive path/stat metadata decides staleness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07-01 | Tampering | `loadWorldStateWithCache` | mitigate | Cache metadata must match pack set, cache version, and recursive mtime+size hash before use; otherwise rebuild. |
| T-23-07-02 | Elevation of Privilege | cache path resolution | mitigate | Use `env-paths` cache directory only; never write into pack source or arbitrary author-controlled paths. |
| T-23-07-03 | Denial of Service | recursive hash scan | mitigate | Deterministic traversal of expected pack files; tests cover deletes/adds without infinite traversal. |
</threat_model>

<verification>
Run `/Users/makoto/.bun/bin/bun test src/world-packs/cache.test.ts src/paths.test.ts src/world-packs/loader.test.ts`.
</verification>

<success_criteria>
- Warm cache loader function path is measured <5ms.
- Cache files live only in user cache dir via `env-paths`.
- Any pack file change invalidates the merged cache.
</success_criteria>

<output>
After completion, create `.planning/phases/23-world-pack-platform/23-07-SUMMARY.md`.
</output>
