---
phase: 23-world-pack-platform
plan: 10
type: execute
wave: 10
depends_on:
  - 23-09
files_modified:
  - src/codex/query.ts
  - src/codex/query.test.ts
  - src/engine/world-memory-recorder.ts
  - src/engine/world-memory-recorder.test.ts
  - src/engine/quest-system.ts
  - src/engine/quest-system.test.ts
  - package.json
autonomous: true
requirements:
  - WPK-01
  - WPK-02
  - WPK-03
  - WPK-04
  - WPK-05
  - WPK-06
  - WPK-07
  - WPK-08
must_haves:
  truths:
    - "Runtime lookups query namespaced WorldState entries without falling back to ambiguous bare cross-pack resolution."
    - "World memory and quest runtime paths use normalized `@classic_fantasy/...` IDs after pack boot and save migration."
    - "Pack layout is included in package output and final verification proves Classic Fantasy plus a second pack load together."
  artifacts:
    - path: "src/codex/query.ts"
      provides: "Namespace-aware exact lookup helpers without cross-pack guessing"
    - path: "src/engine/world-memory-recorder.ts"
      provides: "World memory seeding from namespaced WorldState entries"
    - path: "src/engine/quest-system.ts"
      provides: "Quest runtime lookup compatibility with namespaced refs"
    - path: "package.json"
      provides: "Package files/build metadata including world-data pack layout"
  key_links:
    - from: "src/codex/query.ts"
      to: "src/world-packs/namespace.ts"
      via: "exact namespaced lookup and optional current-pack normalization only"
      pattern: "isNamespacedId|toNamespacedId"
    - from: "src/engine/world-memory-recorder.ts"
      to: "WorldState.entries"
      via: "seed memory from namespaced codex entries"
      pattern: "@classic_fantasy|entries"
    - from: "package.json"
      to: "world-data/packs/classic_fantasy"
      via: "files/build package inclusion"
      pattern: "world-data"
---

<objective>
Complete runtime lookup compatibility, package metadata, and final Phase 23 integration verification.

Purpose: After boot and save migration, engine systems and distribution metadata must consistently use the namespaced pack layout so the feature ships as a working platform, not isolated services.
Output: Focused runtime lookup updates, package file inclusion, final two-pack/cache/save verification gates.
</objective>


<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-09-SUMMARY.md
@src/codex/query.ts
@src/engine/world-memory-recorder.ts
@src/engine/quest-system.ts
@package.json


<interfaces>
From Plan 23-01 expected namespace exports:
```typescript
export function isNamespacedId(value: string): boolean;
export function toNamespacedId(packId: string, localId: string): `@${string}/${string}`;
export const CLASSIC_FANTASY_PACK_ID = "classic_fantasy";
```

From Plan 23-08 runtime state:
```typescript
export interface WorldState {
  readonly entries: ReadonlyMap<string, CodexEntry>;
  readonly packOrder: readonly string[];
  readonly sourceFiles: readonly string[];
}
```
</interfaces>
</context>


<tasks>


<task type="auto" tdd="true">
  <name>Task 1: Update runtime lookup surfaces for namespaced WorldState</name>
  <files>src/codex/query.ts, src/codex/query.test.ts, src/engine/world-memory-recorder.ts, src/engine/world-memory-recorder.test.ts, src/engine/quest-system.ts, src/engine/quest-system.test.ts</files>
  <behavior>
    - `queryById(entries, "@classic_fantasy/...")` returns exact namespaced entries.
    - Any compatibility helper for current-pack local IDs requires an explicit current pack and never guesses across packs per D-07.
    - World memory seeding records namespaced source IDs and does not create duplicate bare-ID memory entries.
    - Quest lookup tests use namespaced quest/NPC/location refs after migration and loader normalization.
  </behavior>
  <action>Update runtime lookup helpers and the highest-risk consumers identified in research: `queryById`, world memory seeding, and quest runtime lookup. Prefer exact namespaced lookups; if an ergonomic same-pack helper is needed, require an explicit `currentPackId` and document that it is not cross-pack resolution. Update tests and fixtures to use `@classic_fantasy/...` values. Do not add last-wins fallback, registry lookup, marketplace behavior, or optional dependency semantics.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/codex/query.test.ts src/engine/world-memory-recorder.test.ts src/engine/quest-system.test.ts</automated>
  </verify>
  <done>Core runtime lookup surfaces operate on namespaced WorldState data and reject ambiguous bare cross-pack assumptions.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 2: Update package inclusion and run final integration gates</name>
  <files>package.json</files>
  <behavior>
    - Package metadata includes the new `world-data/packs/classic_fantasy` layout.
    - Classic Fantasy validates as `@classic_fantasy`.
    - A temp second pack can load alongside Classic Fantasy without entity ID collision.
    - Warm cache, save migration, authoring CLI, build, and typecheck all pass final gates.
  </behavior>
  <action>Update `package.json` `files`/build-related metadata only if the existing package inclusion would miss the new pack layout. Add no new external dependencies. Run final verification commands and fix only issues inside Phase 23 scope. The final tests must prove WPK-01..WPK-08 together: manifest/namespace, loader/toposort, CLI tooling, pre-React boot, cache, save migration, and decoupled world-data paths.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test && /Users/makoto/.bun/bin/bun run typecheck && /Users/makoto/.bun/bin/bun run build</automated>
  </verify>
  <done>Full suite, typecheck, and build pass; package metadata includes pack layout; final integration evidence covers all Phase 23 requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| namespaced WorldState → runtime systems | Engine systems trust IDs from loaded packs and migrated saves. |
| package metadata → published artifact | Distribution packaging decides which world data ships to users. |
| final integration tests → release confidence | Gaps here can hide cross-plan regressions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10-01 | Spoofing | runtime lookup helpers | mitigate | Exact namespaced lookup first; any local lookup requires explicit current pack and never guesses across packs. |
| T-23-10-02 | Tampering | package file inclusion | mitigate | Verify build/package metadata includes the new pack layout and no unintended registry/marketplace files. |
| T-23-10-03 | Repudiation | final verification | mitigate | Run full suite/typecheck/build and record evidence in the plan summary. |
</threat_model>


<verification>
Run `/Users/makoto/.bun/bin/bun test && /Users/makoto/.bun/bin/bun run typecheck && /Users/makoto/.bun/bin/bun run build` after focused runtime tests pass.
</verification>


<success_criteria>
- Runtime lookup and memory/quest consumers operate with namespaced IDs.
- Package metadata ships the new pack layout.
- Final gates prove WPK-01 through WPK-08 and D-01 through D-18 are implemented without deferred Phase 24/registry/marketplace scope.
</success_criteria>


<output>
After completion, create `.planning/phases/23-world-pack-platform/23-10-SUMMARY.md`.
</output>
