---
phase: 23-world-pack-platform
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/world-packs/loader.ts
  - src/world-packs/loader.test.ts
  - src/codex/loader.ts
autonomous: true
requirements:
  - WPK-02
  - WPK-03
must_haves:
  truths:
    - "Multiple packs load in dependency order and duplicate fully-qualified IDs fail loudly."
    - "Same-pack bare refs normalize to @pack/entity_id while explicit cross-pack refs remain explicit."
    - "The loader is pure engine-layer code with no React dependency."
  artifacts:
    - path: "src/world-packs/loader.ts"
      provides: "Pure WorldPackLoader with manifest parsing, topological sort, strict additive merge, and ref normalization"
      exports: ["loadWorldPacks", "topologicallySortPacks", "loadPackManifest"]
    - path: "src/codex/loader.ts"
      provides: "Reusable codex YAML/schema helpers for pack loading without changing legacy behavior"
  key_links:
    - from: "src/world-packs/loader.ts"
      to: "src/world-packs/namespace.ts"
      via: "normalizeEntryRefs before merge"
      pattern: "normalizeEntryRefs"
    - from: "src/world-packs/loader.ts"
      to: "src/codex/loader.ts"
      via: "reuse exported codex YAML/schema loaders"
      pattern: "loadCodexFile"
---

<objective>
Implement the pure WorldPackLoader, dependency ordering, and strict additive merge.

Purpose: Pack composition must be collision-free and deterministic before validation, CLI tooling, cache, or App boot can rely on it.
Output: Loader service and tests with no Classic Fantasy data restructure in this plan.
</objective>


<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-01-SUMMARY.md
@src/codex/loader.ts
@src/codex/schemas/entry-types.ts
@src/codex/schemas/relationship.ts


<interfaces>
From Plan 23-01 expected exports:
```typescript
export interface WorldState {
  readonly entries: ReadonlyMap<string, CodexEntry>;
  readonly packOrder: readonly string[];
  readonly sourceFiles: readonly string[];
  readonly manifestHash?: string;
}
export function normalizeEntryRefs<T>(value: T, packId: string): T;
export function toNamespacedId(packId: string, localId: string): `@${string}/${string}`;
```

Existing codex loader exports to preserve or expose:
```typescript
export async function loadCodexFile(filePath: string): Promise<CodexEntry[]>;
export async function loadRelationships(filePath: string): Promise Promise<RelationshipEdge[]>;
export async function loadAllCodex(codexDir: string): Promise<Map<string, CodexEntry>>;
```
</interfaces>
</context>


<tasks>


<task type="auto" tdd="true">
  <name>Task 1: Build manifest loading and dependency toposort</name>
  <files>src/world-packs/loader.ts, src/world-packs/loader.test.ts</files>
  <behavior>
    - `loadPackManifest(packDir)` reads `manifest.yaml`, validates with `WorldPackManifestSchema`, and records packDir/source path.
    - Required dependencies are topologically sorted per D-06.
    - Missing dependencies and dependency cycles fail with deterministic pack-id paths.
    - Optional dependencies are not implemented in Phase 23.
  </behavior>
  <action>Create `src/world-packs/loader.ts` as a side-effect-contained async service that accepts explicit `packDirs` and never imports React, App, or global stores. Implement `loadPackManifest(packDir)` and `topologicallySortPacks(manifests)` using only required dependencies per D-06. Errors must be deterministic and author-actionable per D-12. Do not restructure Classic Fantasy data here; tests should use temp pack fixtures.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/loader.test.ts src/world-packs/manifest-schema.test.ts</automated>
  </verify>
  <done>Pack manifests load from explicit directories and dependency ordering/missing/cycle behavior is fully covered without touching world-data files.</done>
</task>


<task type="auto" tdd="true">
  <name>Task 2: Merge pack codex entries with strict namespace rules</name>
  <files>src/world-packs/loader.ts, src/world-packs/loader.test.ts, src/codex/loader.ts</files>
  <behavior>
    - Loading one pack returns entries keyed by `@pack/id` per D-01.
    - Loading two independent packs returns both sets without collision.
    - Duplicate fully-qualified IDs throw; no last-wins behavior per D-05.
    - Same-pack bare refs normalize; explicit cross-pack refs survive unchanged per D-04/D-07.
  </behavior>
  <action>Implement `loadPackCodex(pack)` and `loadWorldPacks({ packDirs })`. Reuse `loadCodexFile` and `loadRelationships`; adapt `src/codex/loader.ts` only to export reusable helpers while preserving existing behavior. Merge is strict additive: if a normalized fully-qualified ID already exists, throw with both pack IDs and source context. Include `sourceFiles` for cache invalidation in later plans. Do not implement override, patch, optional dependency, registry, or last-wins semantics.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/loader.test.ts src/world-packs/namespace.test.ts src/codex/loader.test.ts</automated>
  </verify>
  <done>`loadWorldPacks` returns deterministic `WorldState` with namespaced entries, dependency-ordered packs, strict duplicate errors, normalized refs, and no React dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pack directory path → filesystem scan | User-provided or project-local pack paths drive file reads. |
| pack YAML → runtime WorldState | Author-controlled content becomes game state consumed by engine systems. |
| dependency declarations → load order | Manifest dependencies determine composition order and availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | `loadWorldPacks` merge map | mitigate | Reject duplicate fully-qualified IDs; never overwrite existing entries. |
| T-23-02-02 | Denial of Service | dependency graph | mitigate | Detect missing dependencies and cycles before loading content. |
| T-23-02-03 | Spoofing | cross-pack refs | mitigate | Require explicit `@pack/entity_id` for cross-pack refs and preserve them during normalization. |
</threat_model>


<verification>
Run `/Users/makoto/.bun/bin/bun test src/world-packs/loader.test.ts src/world-packs/namespace.test.ts src/codex/loader.test.ts`.
</verification>


<success_criteria>
- WPK-02 and WPK-03 loader behavior is covered by focused tests.
- Loader is pure engine-layer code and can be called before React.
- D-04, D-05, D-06, D-07 are enforced in loader tests.
</success_criteria>


<output>
After completion, create `.planning/phases/23-world-pack-platform/23-02-SUMMARY.md`.
</output>
