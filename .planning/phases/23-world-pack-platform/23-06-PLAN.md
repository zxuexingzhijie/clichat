---
phase: 23-world-pack-platform
plan: 06
type: execute
wave: 6
depends_on:
  - 23-05
files_modified:
  - src/world-packs/init.ts
  - src/world-packs/init.test.ts
  - src/world-packs/diff.ts
  - src/world-packs/diff.test.ts
  - src/cli.ts
  - src/cli.test.ts
autonomous: true
requirements:
  - WPK-04
must_haves:
  truths:
    - "Content authors can run `chronicle init` to create a minimal valid pack."
    - "Content authors can run `chronicle validate` to receive deterministic deep validation results."
    - "Content authors can run `chronicle diff` to see entity-grouped field-level changes."
  artifacts:
    - path: "src/world-packs/init.ts"
      provides: "Minimal valid pack scaffold service"
      exports: ["initWorldPack", "createMinimalPackFiles"]
    - path: "src/world-packs/diff.ts"
      provides: "Deterministic entity/group/field diff service"
      exports: ["diffWorldPacks", "formatPackDiff"]
    - path: "src/cli.ts"
      provides: "Commander authoring subcommands init, validate, diff"
  key_links:
    - from: "src/cli.ts"
      to: "src/world-packs/init.ts"
      via: "chronicle init command action"
      pattern: "command\\('init'\\)"
    - from: "src/cli.ts"
      to: "src/world-packs/validate.ts"
      via: "chronicle validate command action"
      pattern: "command\\('validate'\\)"
    - from: "src/cli.ts"
      to: "src/world-packs/diff.ts"
      via: "chronicle diff command action"
      pattern: "command\\('diff'\\)"
---

<objective>
Add authoring SDK CLI commands for scaffold, validation, and entity-level diff.

Purpose: World pack authors need deterministic local tooling before loading their content at runtime.
Output: `chronicle init`, `chronicle validate`, `chronicle diff` services, CLI routing, and tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-world-pack-platform/23-CONTEXT.md
@.planning/phases/23-world-pack-platform/23-RESEARCH.md
@.planning/phases/23-world-pack-platform/23-PATTERNS.md
@.planning/phases/23-world-pack-platform/23-VALIDATION.md
@.planning/phases/23-world-pack-platform/23-05-SUMMARY.md
@src/cli.ts
@src/engine/branch-diff.ts
@src/persistence/save-file-manager.ts

<interfaces>
From Plans 23-02/23-03 expected exports:
```typescript
export async function validateWorldPacks(options: ValidateWorldPacksOptions): Promise<ValidationResult>;
export function formatValidationResult(result: ValidationResult): string[];
export async function loadWorldPacks(options: LoadWorldPacksOptions): Promise<WorldState>;
```

Existing CLI shape:
```typescript
const program = new Command()
  .name('chronicle')
  .description('AI-driven CLI interactive novel game')
  .version('1.1.0')
  .option('--world-dir <path>', 'Custom world data directory')
  .action(async (opts) => { await import('./index'); });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement `chronicle init` minimal valid pack scaffold</name>
  <files>src/world-packs/init.ts, src/world-packs/init.test.ts, src/cli.ts, src/cli.test.ts</files>
  <behavior>
    - `initWorldPack({ targetDir, id })` writes `manifest.yaml` and required `codex/` YAML files.
    - Scaffold includes one useful example/commented location, NPC, item, and quest per D-09.
    - Immediately validating the scaffold with `validateWorldPacks` succeeds.
    - Path traversal or existing non-empty target directory fails without overwriting author files.
  </behavior>
  <action>Create `src/world-packs/init.ts` with injectable filesystem helpers. The minimal pack must include `manifest.yaml`, `codex/locations.yaml`, `codex/npcs.yaml`, `codex/items.yaml`, and `codex/quests.yaml`; include comments/examples while remaining valid YAML. Wire top-level `chronicle init <targetDir> --id <pack_id> --name <name>` in `src/cli.ts`. Keep output deterministic and author-facing per D-12; no template variants, no registry, no marketplace, no Phase 24 polish. Add CLI tests by invoking the Commander setup in a testable function or by extracting `createProgram()` so tests can parse argv without starting React.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/init.test.ts src/cli.test.ts src/world-packs/validate.test.ts</automated>
  </verify>
  <done>`chronicle init` scaffolds a minimal valid pack exactly per D-09 and refuses unsafe writes; tests validate the scaffold through the real validator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `chronicle validate` and `chronicle diff` authoring commands</name>
  <files>src/world-packs/diff.ts, src/world-packs/diff.test.ts, src/cli.ts, src/cli.test.ts</files>
  <behavior>
    - `chronicle validate <packDir>` prints stable success/failure lines and sets non-zero exitCode on validation failure.
    - `chronicle diff <oldPackDir> <newPackDir>` groups added/removed/changed output by entity type and entity ID.
    - Diff includes field-level summaries for changed entities and stable sorting per D-11/D-12.
    - Diff is structured, not raw YAML diff and not gameplay-impact prose.
  </behavior>
  <action>Create `src/world-packs/diff.ts` using the loader to parse both pack sets, compare maps by fully-qualified ID, group by entry `type`, and report `added`, `removed`, and `changed` with changed field paths. Use deterministic sorting by type then ID then field path. Wire `chronicle validate [packDir...]` to `validateWorldPacks`/`formatValidationResult` and `chronicle diff <before> <after>` to `diffWorldPacks`/`formatPackDiff`. Preserve default no-subcommand behavior for game boot. Do not add colored/animated output; errors should be actionable and stable.</action>
  <verify>
    <automated>/Users/makoto/.bun/bin/bun test src/world-packs/diff.test.ts src/world-packs/validate.test.ts src/cli.test.ts</automated>
  </verify>
  <done>`chronicle validate` performs deep validation from Plan 23-03 via CLI, and `chronicle diff` outputs entity-grouped field summaries exactly per D-10/D-11/D-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → filesystem writes | `chronicle init` writes author files to a user-provided path. |
| CLI args → filesystem reads | `validate` and `diff` read user-provided pack directories. |
| validation output → author decisions | CLI errors guide content authors and must not hide failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Tampering | `initWorldPack` target path | mitigate | Reject `..` traversal and existing non-empty directories; create only requested scaffold files. |
| T-23-06-02 | Repudiation | CLI validation result | mitigate | Set `process.exitCode = 1` on invalid packs and print deterministic error lines. |
| T-23-06-03 | Information Disclosure | diff output | accept | Diff only reads explicitly provided local pack directories and outputs entity metadata intended for authors. |
</threat_model>

<verification>
Run `/Users/makoto/.bun/bin/bun test src/world-packs/init.test.ts src/world-packs/validate.test.ts src/world-packs/diff.test.ts src/cli.test.ts`.
</verification>

<success_criteria>
- `chronicle init` creates a valid minimal pack.
- `chronicle validate` catches deep validation failures before runtime.
- `chronicle diff` shows deterministic added/removed/changed entity and field summaries.
- No deferred template variants, registry, marketplace, optional dependencies, or gameplay-impact prose reports are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/23-world-pack-platform/23-06-SUMMARY.md`.
</output>
