---
phase: 08-narrative-character-creation
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - src/ui/screens/character-creation-screen.tsx
  - src/state/character-creation-store.ts
  - src/state/new-stores.test.ts
autonomous: true
requirements: [NCC-01]
must_haves:
  truths:
    - "CharacterCreationScreen component file is deleted"
    - "character-creation-store.ts file is deleted"
    - "No file in src/ imports CharacterCreationScreen or characterCreationStore"
    - "All tests pass after deletion"
  artifacts:
    - path: "src/ui/screens/character-creation-screen.tsx"
      provides: "DELETED"
    - path: "src/state/character-creation-store.ts"
      provides: "DELETED"
  key_links: []
---

<objective>
Remove the old character creation screen and store that are replaced by the narrative creation system. Clean up any remaining references.

Purpose: Per D-08, the old CharacterCreationScreen and character-creation-store.ts are removed entirely. The engine (character-creation.ts) is preserved. This cleanup ensures no dead code remains and tests reference only the new narrative creation flow.

Output: Two files deleted, test file updated, codebase clean of old creation references.
</objective>

<execution_context>
@.planning/phases/08-narrative-character-creation/08-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-narrative-character-creation/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old creation files + update affected tests</name>
  <files>src/ui/screens/character-creation-screen.tsx, src/state/character-creation-store.ts, src/state/new-stores.test.ts</files>
  <read_first>
    - src/state/character-creation-store.ts (the file being deleted -- confirm no other consumers)
    - src/state/new-stores.test.ts (contains characterCreationStore test that must be removed)
    - src/app.tsx (confirm it no longer imports CharacterCreationScreen after Plan 02+03)
  </read_first>
  <action>
    **1. Grep the entire `src/` directory for remaining references:**

    Search for these patterns and fix any remaining references:
    - `CharacterCreationScreen` -- should have 0 matches after Plan 02+03 updated app.tsx
    - `character-creation-screen` (import path) -- should have 0 matches
    - `characterCreationStore` -- should only be in character-creation-store.ts and new-stores.test.ts
    - `character-creation-store` (import path) -- should only be in new-stores.test.ts

    **2. Delete `src/ui/screens/character-creation-screen.tsx`:**
    ```bash
    rm src/ui/screens/character-creation-screen.tsx
    ```

    **3. Delete `src/state/character-creation-store.ts`:**
    ```bash
    rm src/state/character-creation-store.ts
    ```

    **4. Update `src/state/new-stores.test.ts`:**

    Remove the entire test block that imports and tests `characterCreationStore`:
    - Remove `import { characterCreationStore } from './character-creation-store'`
    - Remove the `describe('characterCreationStore', ...)` block and all its test cases
    - Keep all other store tests in the file intact

    **5. Final verification grep:**

    After deletion, confirm zero matches for:
    - `CharacterCreationScreen` anywhere in `src/`
    - `character-creation-screen` anywhere in `src/`
    - `characterCreationStore` anywhere in `src/`
    - `character-creation-store` anywhere in `src/`
    - `character_creation` as a string literal anywhere in `src/` (should all be replaced by `narrative_creation` from Plan 02)

    If any matches remain, fix them (likely in test files or comments).
  </action>
  <verify>
    <automated>cd /Users/makoto/Downloads/work/cli && bun test -x 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/ui/screens/character-creation-screen.tsx does NOT exist (ls returns error)
    - src/state/character-creation-store.ts does NOT exist (ls returns error)
    - grep for 'CharacterCreationScreen' in src/ returns 0 matches
    - grep for 'characterCreationStore' in src/ returns 0 matches
    - grep for 'character_creation' as string literal in src/ returns 0 matches (all replaced by narrative_creation)
    - `bun test` passes with all tests green
    - src/engine/character-creation.ts still exists (engine is preserved per D-08)
    - src/engine/character-creation.test.ts still exists and passes
  </acceptance_criteria>
  <done>Old creation screen and store deleted, no dangling references remain, all tests pass, character-creation.ts engine preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. This plan only deletes code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Tampering | Deletion of old files | accept | No security impact. Removal of dead code reduces attack surface marginally. Verified by full test suite pass. |
</threat_model>

<verification>
```bash
cd /Users/makoto/Downloads/work/cli && bun test -x && echo "---" && ls src/ui/screens/character-creation-screen.tsx 2>&1 && echo "---" && ls src/state/character-creation-store.ts 2>&1
```
</verification>

<success_criteria>
- Both old files deleted
- Zero references to old creation system in src/
- Full test suite passes
- character-creation.ts engine file preserved and its tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-narrative-character-creation/08-04-SUMMARY.md`
</output>
