---
phase: 03-persistence-world
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/data/codex/locations.yaml
  - src/data/codex/npcs.yaml
  - src/data/codex/factions.yaml
  - src/data/codex/quests.yaml
autonomous: true
requirements: [CONT-01, CONT-03]

must_haves:
  truths:
    - "8-10 locations exist across 黑松镇 and surrounding wilderness, each with region, danger_level, exits, notable_npcs, objects"
    - "3-4 factions exist (卫队/影子公会/商人协会/神殿), each with territory, alignment, goals, rivals"
    - "15+ named NPCs distributed across locations with backstories, goals, personality_tags, initial_disposition"
    - "1 main quest skeleton (5-8 stages) and 5+ side quest templates exist in quests.yaml"
    - "All YAML entries validate against CodexEntrySchema at codex load time"
  artifacts:
    - path: "src/data/codex/locations.yaml"
      provides: "8-10 location entries"
      contains: "loc_north_gate"
    - path: "src/data/codex/npcs.yaml"
      provides: "15+ NPC entries"
      contains: "npc_guard"
    - path: "src/data/codex/factions.yaml"
      provides: "3-4 faction entries"
      contains: "faction_guard"
    - path: "src/data/codex/quests.yaml"
      provides: "Quest template entries (main + side quests)"
      contains: "quest_main_01"
  key_links:
    - from: "src/data/codex/quests.yaml"
      to: "src/codex/schemas/entry-types.ts"
      via: "QuestTemplateSchema loaded by loadAllCodex()"
      pattern: "type: quest"
    - from: "src/data/codex/locations.yaml"
      to: "loc_north_gate"
      via: "exits reference other loc_ ids defined in same file"
      pattern: "exits:"
---

<objective>
Expand the world content YAML to fulfill CONT-01 (first region: 8-12 locations, 3-4 factions) and CONT-03 (quest content: 1 main quest + 5-8 side quest templates). This is a pure data authoring task — no TypeScript code changes.

Purpose: Provide the world the player can actually explore in Phase 3 and beyond.
Output: Fully populated YAML files for locations, NPCs, factions, and quest templates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-persistence-world/03-CONTEXT.md
@.planning/phases/03-persistence-world/03-RESEARCH.md
</context>

<interfaces>
<!-- Existing YAML structure — all new entries must match these field shapes exactly -->

From src/codex/schemas/entry-types.ts — LocationSchema fields:
```
id, name, type: "location", tags, description, epistemic, region, danger_level (0-10), exits (array of loc_ ids), notable_npcs (array of npc_ ids), objects (array of strings)
```

From src/codex/schemas/entry-types.ts — NpcSchema fields:
```
id, name, type: "npc", tags, description, epistemic, location_id, personality_tags, goals, backstory, initial_disposition (-1.0 to 1.0)
```

From src/codex/schemas/entry-types.ts — FactionSchema fields:
```
id, name, type: "faction", tags, description, epistemic, territory, alignment, goals, rivals (array of faction ids)
```

From src/codex/schemas/entry-types.ts — QuestTemplateSchema fields (added in Plan 01):
```
id, name, type: "quest", tags, description, epistemic, quest_type ("main"|"side"|"faction"), region (optional), required_npc_id (optional), min_reputation (optional), stages (array), rewards
stages[]: { id, description, objectives: [{id, type, targetId?, description}], nextStageId (nullable), completionCondition? }
rewards: { gold?, items?, reputation_delta?, relation_delta? }
```

Epistemic template (copy for all entries):
```yaml
epistemic:
  authority: canonical_truth
  truth_status: "true"
  scope: regional
  visibility: public
  confidence: 1.0
  source_type: authorial
  known_by: []
  contradicts: []
  volatility: stable
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Expand locations.yaml and factions.yaml to first region</name>
  <files>
    src/data/codex/locations.yaml,
    src/data/codex/factions.yaml
  </files>
  <read_first>
    - src/data/codex/locations.yaml (full existing content — do not duplicate existing entries)
    - src/data/codex/factions.yaml (full existing content — do not duplicate existing entries)
    - src/codex/schemas/entry-types.ts (LocationSchema and FactionSchema for field reference)
  </read_first>
  <action>
  Expand locations.yaml to total 9 locations covering the first region 黑松镇 area:
  - Keep existing: loc_north_gate, loc_tavern (+ any others already defined)
  - Add remaining to reach 9 total:
    - loc_main_street — 黑松镇主街, danger 1, exits to all town districts
    - loc_blacksmith — 铁匠铺, danger 0, exits: loc_main_street, notable_npcs: npc_blacksmith
    - loc_market — 集市广场, danger 1, exits: loc_main_street, notable_npcs: npc_merchant
    - loc_temple — 神殿, danger 0, exits: loc_main_street, notable_npcs: npc_priest
    - loc_forest_road — 黑松林道, danger 4, exits: loc_north_gate, loc_abandoned_camp
    - loc_abandoned_camp — 废弃营地, danger 6, exits: loc_forest_road, loc_dark_cave
    - loc_dark_cave — 黑暗洞穴, danger 8, exits: loc_abandoned_camp, notable_npcs: []
    All exits must reference valid loc_ ids within the file. Use region: "黑松镇" for town locations, region: "黑松林" for wilderness locations.

  Expand factions.yaml to 4 factions:
  - Keep existing: faction_guard, faction_shadow_guild (+ any others)
  - Add remaining to reach 4 total:
    - faction_merchants — 黑松商人协会, territory: 黑松镇·集市, alignment: neutral, goals: [trade, profit protection], rivals: [faction_shadow_guild]
    - faction_temple — 神殿教团, territory: 黑松镇·神殿, alignment: lawful good, goals: [maintain order, heal the sick], rivals: []
  </action>
  <verify>
    <automated>bun test src/codex/loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- id: loc_" src/data/codex/locations.yaml` returns 9 or more
    - `grep -c "^- id: faction_" src/data/codex/factions.yaml` returns 4 or more
    - `bun test src/codex/loader.test.ts` passes — loader validates all entries
    - Every `exits:` entry in locations.yaml references a loc_ id that exists in the same file
  </acceptance_criteria>
  <done>
    9 locations and 4 factions in YAML, all passing codex loader validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand npcs.yaml and create quests.yaml</name>
  <files>
    src/data/codex/npcs.yaml,
    src/data/codex/quests.yaml
  </files>
  <read_first>
    - src/data/codex/npcs.yaml (full existing content — do not duplicate existing NPCs)
    - src/codex/schemas/entry-types.ts (NpcSchema and QuestTemplateSchema fields)
    - src/data/codex/locations.yaml (to reference valid location_id values)
    - src/data/codex/factions.yaml (to reference valid faction ids in quest rewards)
  </read_first>
  <action>
  Expand npcs.yaml to 15 named NPCs (keep all existing, add to reach 15+):
  - npc_guard (existing) — location: loc_north_gate
  - npc_bartender (existing) — location: loc_tavern
  - npc_blacksmith (existing or add) — location: loc_blacksmith, goals: [forge weapons, find rare ore], initial_disposition: 0.1
  - npc_merchant — location: loc_market, goals: [sell goods, track debts], personality: [greedy, observant], initial_disposition: 0.0
  - npc_priest — location: loc_temple, goals: [protect townspeople, seek lost relic], personality: [pious, secretive], initial_disposition: 0.3
  - npc_innkeeper — location: loc_tavern, goals: [keep peace, gather information], personality: [friendly, gossipy], initial_disposition: 0.2
  - npc_mysterious_stranger — location: loc_tavern, goals: [remain anonymous, investigate conspiracy], personality: [guarded, perceptive], initial_disposition: -0.1
  - npc_elder — location: loc_main_street, goals: [protect town history, warn of ancient danger], personality: [wise, cryptic], initial_disposition: 0.4
  - npc_scout — location: loc_forest_road, goals: [patrol forest, report dangers], personality: [alert, laconic], initial_disposition: 0.1
  - npc_deserter — location: loc_abandoned_camp, goals: [hide from guard, find redemption], personality: [fearful, remorseful], initial_disposition: -0.3
  Add 5+ more NPCs as needed to reach 15 total. Each must have: valid location_id (matching loc_ ids from locations.yaml), personality_tags (2-4 strings), goals (2-3 strings), backstory (1-2 sentence Chinese string), initial_disposition (-1.0 to 1.0 float).

  Create src/data/codex/quests.yaml with the following content (YAML array):

  1 main quest — quest_main_01: "黑松镇的阴影" (Shadow over Blackpine)
  - quest_type: main, region: 黑松镇
  - 6 stages: (1) 到酒馆打听消息 [talk npc_bartender], (2) 调查废弃营地 [visit_location loc_abandoned_camp], (3) 拜访神殿寻找线索 [talk npc_priest], (4) 找到失踪卫兵 [find_item item_guard_badge or defeat enemy], (5) 对抗幕后黑手 [defeat_enemy], (6) 向镇长汇报 [talk npc_elder]
  - rewards: { gold: 200, reputation_delta: { faction_guard: 20, faction_shadow_guild: -15 } }

  5 side quest templates (side quest type):
  - quest_side_missing_ore: 铁匠的失踪原料 — required_npc_id: npc_blacksmith, 3 stages
  - quest_side_overdue_debt: 欠款追讨 — required_npc_id: npc_merchant, 2 stages, min_reputation: -20
  - quest_side_temple_relic: 神殿遗物 — required_npc_id: npc_priest, 4 stages
  - quest_side_forest_patrol: 森林巡逻 — region: 黑松林, 3 stages
  - quest_side_deserter_plea: 逃兵的请求 — required_npc_id: npc_deserter, 3 stages, min_reputation: -30

  Every quest entry must have: id, name, type: quest, tags, description, epistemic (use template above), quest_type, stages (each with id/description/objectives/nextStageId), rewards.
  </action>
  <verify>
    <automated>bun test src/codex/loader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- id: npc_" src/data/codex/npcs.yaml` returns 15 or more
    - `grep -c "^- id: quest_" src/data/codex/quests.yaml` returns 6 or more (1 main + 5 side)
    - `grep "quest_type: main" src/data/codex/quests.yaml` matches quest_main_01
    - `bun test src/codex/loader.test.ts` passes — all quest entries validate against QuestTemplateSchema
    - No NPC has a location_id that does not exist in locations.yaml
  </acceptance_criteria>
  <done>
    15+ NPCs in npcs.yaml; quests.yaml with 1 main quest (6 stages) and 5 side quests; all entries pass codex loader validation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML files → codex loader | Authored static content; validated at load by CodexEntrySchema.safeParse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | quests.yaml | mitigate | Zod safeParse validates all quest entries at startup (per existing loader pattern); invalid entries throw with entry id in error message |
| T-03-04 | Information Disclosure | NPC backstories | accept | Static authored content; no player secrets embedded in YAML |
</threat_model>

<verification>
- `bun test src/codex/loader.test.ts` — passes with quest entries loading
- `bun test` — full suite green
- `grep -c "^- id: loc_" src/data/codex/locations.yaml` — returns 9+
- `grep -c "^- id: faction_" src/data/codex/factions.yaml` — returns 4+
- `grep -c "^- id: npc_" src/data/codex/npcs.yaml` — returns 15+
- `grep -c "^- id: quest_" src/data/codex/quests.yaml` — returns 6+
</verification>

<success_criteria>
- 9+ locations covering town + wilderness in YAML
- 4 factions in YAML
- 15+ NPCs with valid location_id references in YAML
- quests.yaml with 1 main quest (5+ stages) and 5+ side quest templates
- All YAML entries validate via codex loader (bun test src/codex/loader.test.ts passes)
</success_criteria>

<output>
After completion, create `.planning/phases/03-persistence-world/03-02-SUMMARY.md`
</output>
