---
status: complete
quick_id: 20260429-001
slug: memory-wiring-fix
date: 2026-04-29
commit: 79123eb
---

## Summary

修复了记忆系统与对话路径之间的3个接线缺口：

### Gap 1 — 记忆截断 slice(0,3) → slice(0,8)
`src/ai/prompts/npc-system.ts`：将记忆上限从3提高到8，并拆分出独立的 `archiveSummary` 和 `relevantCodex` prompt section。

### Gap 2 — archiveSummary 从未传给 LLM
`src/engine/dialogue-manager.ts`：新增 `buildNpcCallOptions` helper，从 `npcMemoryStore` 读取 `archiveSummary`，通过 `NpcActorOptions` 传入 `generateNpcDialogue`。3处调用全部接入。

### Gap 3 — Codex 过滤从未接到对话路径
`buildNpcCallOptions` 同时构建 `NpcFilterContext` 并调用 `filterCodexForNpc`，将前3条相关世界知识的描述作为 `relevantCodex` 传入 prompt。

### Bonus — 修复 recentMemories mutation
`writeMemory` 中 `existing.recentMemories.push(newEntry)` → spread 赋值，符合项目不变量规范。

## Tests

- tsc: clean
- 959 pass, 1 fail (pre-existing: use-game-input.test.ts 'i' → null)
