---
phase: 18-multi-turn-dialogue
plan: "03"
type: tdd
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - src/ui/hooks/use-npc-dialogue.ts
  - src/ui/hooks/use-npc-dialogue.test.ts
  - src/ui/screens/narrative-creation-screen.tsx
autonomous: true
requirements: [DIAL-03]
must_haves:
  truths:
    - "useNpcDialogue hook 内部持有 messagesRef，跨 startDialogue 调用积累 {role,content}[] 历史"
    - "每轮流式完成后，hook 将 {role:'user',content:playerAction} + {role:'assistant',content:fullText} 追加到 messagesRef"
    - "reset() 只清除流式状态，不清除 messagesRef"
    - "resetMessages() 新 API 允许外部（narrative-creation-screen）显式清空 messages 历史"
    - "narrative-creation-screen.tsx 在守卫对话开始前调用 resetMessages()，保证每次角色创建是全新 session"
    - "NpcDialogueContext.conversationHistory 类型迁移为 {role:'user'|'assistant', content:string}[]"
  artifacts:
    - path: "src/ui/hooks/use-npc-dialogue.ts"
      provides: "messagesRef 积累 + resetMessages() + startDialogue 使用内部 messages"
      contains: "messagesRef"
    - path: "src/ui/hooks/use-npc-dialogue.test.ts"
      provides: "3 轮积累测试 + resetMessages 测试"
      contains: "accumulate\|resetMessages"
    - path: "src/ui/screens/narrative-creation-screen.tsx"
      provides: "loadRetryKey useEffect 调用 resetMessages()"
      contains: "resetMessages"
  key_links:
    - from: "useNpcDialogue completion useEffect"
      to: "messagesRef.current"
      via: "spread append: [...messagesRef.current, {role:'user',...}, {role:'assistant',...}]"
      pattern: "messagesRef\\.current.*role"
    - from: "startDialogue"
      to: "streamNpcDialogue options.conversationHistory"
      via: "messagesRef.current (当前积累)"
      pattern: "messagesRef\\.current"
    - from: "narrative-creation-screen loadRetryKey useEffect"
      to: "npcDialogue.resetMessages()"
      via: "直接调用"
      pattern: "resetMessages"
---

<objective>
实现守卫创建对话的多轮上下文积累（DIAL-03）：useNpcDialogue hook 内部用 messagesRef 跨轮次积累 {role,content}[]，每轮完成后自动追加，startDialogue 使用内部 messages 而非每次重置。narrative-creation-screen 在对话开始前调用 resetMessages() 保证 session 边界。

Purpose: 守卫 4 轮对话的第 3/4 问需要能引用玩家前两轮的回答，而不是每次都独立生成。
Output: use-npc-dialogue.ts 增加 messagesRef + resetMessages；narrative-creation-screen.tsx 调用 resetMessages。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-multi-turn-dialogue/18-CONTEXT.md
@.planning/phases/18-multi-turn-dialogue/18-RESEARCH.md
@.planning/phases/18-multi-turn-dialogue/18-PATTERNS.md
@.planning/phases/18-multi-turn-dialogue/18-P01-SUMMARY.md
@.planning/phases/18-multi-turn-dialogue/18-P02-SUMMARY.md
</context>

<interfaces>
<!-- Wave 1+2 产出的接口（执行器需知道） -->

From src/ui/hooks/use-npc-dialogue.ts (current after Wave 2):
```typescript
export type NpcDialogueContext = {
  readonly npcProfile: NpcProfile;
  readonly scene: string;
  readonly playerAction: string;
  readonly memories: readonly string[];
  readonly archiveSummary?: string;
  readonly relevantCodex?: readonly string[];
  // Wave 2 已迁移类型（若 Wave 2 修改了此字段）：
  readonly conversationHistory?: readonly { readonly role: 'user' | 'assistant'; readonly content: string }[];
};

export type UseNpcDialogueReturn = {
  readonly streamingText: string;
  readonly isStreaming: boolean;
  readonly error: Error | null;
  readonly metadata: NpcDialogue | null;
  readonly startDialogue: (context: NpcDialogueContext) => void;
  readonly skipToEnd: () => void;
  readonly reset: () => void;
  // Wave 3 新增：
  // readonly resetMessages: () => void;
};

// Hook 内部现有 useRef 模式（复制用于 messagesRef）：
const contextRef = useRef<NpcDialogueContext | null>(null);
const completionFiredRef = useRef(false);
// 流式完成时 fullText 来自 streaming.fullTextRef.current
// ctx.playerAction 是当前轮次的玩家动作文本
```

From src/ui/screens/narrative-creation-screen.tsx (current):
```typescript
// 4 处 npcDialogue.reset() 调用位置（reset 只清流式状态，不清 messages）：
// Line 190: handleOptionSelected 中（每轮选择后）
// Line 220: Escape 返回上一轮
// Line 257: handleNameSubmitted 后

// 守卫对话开始点（loadRetryKey useEffect 末尾，line ~86）：
setPhase({ type: 'round_streaming', round: 1 });
// => 在此 setPhase 前调用 npcDialogue.resetMessages()

// startDialogue 调用（line 109-114）：
npcDialogue.startDialogue({
  npcProfile: guardProfile,
  scene: sceneContext,
  playerAction,
  memories: [],
});
// => 无需改动 call site；hook 内部使用 messagesRef.current
```

关键约束（来自 RESEARCH.md Pitfall 5）：
reset() 不能清 messagesRef — narrative-creation-screen 在每轮选择后调用 reset()，
若 reset() 清 messagesRef 则下一轮无历史可用。
reset() 只清流式状态（streaming.reset + setMetadata(null)）。
messagesRef 只在 resetMessages() 中清空。
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: [RED] use-npc-dialogue.test.ts — messagesRef 积累 + resetMessages 测试</name>
  <files>src/ui/hooks/use-npc-dialogue.test.ts</files>
  <read_first>
    - src/ui/hooks/use-npc-dialogue.ts （完整读取——理解现有 hook 结构和 useRef 模式）
    - src/ai/roles/npc-actor.ts （了解 streamNpcDialogue / generateNpcDialogue 签名，用于 mock 设置）
  </read_first>
  <behavior>
    测试文件不存在（Wave 0 gap），需新建。复制 ai-caller.test.ts 的 mock 模式（top-level mock declarations + mock.module + dynamic import）。

    测试用例（全部预期 RED，因 hook 未实现）：
    1. "messagesRef 在首次 startDialogue 后为空（流式未完成）" — startDialogue 调用后 messagesRef 尚未 append（append 发生在流式完成时）
    2. "流式完成后 messagesRef 含 {role:user} + {role:assistant} 各一条" — 模拟 isStreaming false + fullText 存在
    3. "第二次 startDialogue 时 streamNpcDialogue 收到 messagesRef.current 作为 conversationHistory"
    4. "reset() 不清除 messagesRef" — reset() 后 messagesRef 仍有上一轮记录
    5. "resetMessages() 清空 messagesRef" — 调用后 messagesRef.current.length === 0
  </behavior>
  <action>
  新建 src/ui/hooks/use-npc-dialogue.test.ts。

  Mock 设置参考 ai-caller.test.ts 的 top-level mock 模式：
  ```typescript
  const mockStreamNpcDialogue = mock(() => (async function* () { yield 'hello'; })());
  const mockGenerateNpcDialogue = mock(() => Promise.resolve({ dialogue: 'hello', sentiment: 'neutral', emotionTag: 'neutral', shouldRemember: false }));
  mock.module('../../ai/roles/npc-actor', () => ({
    streamNpcDialogue: mockStreamNpcDialogue,
    generateNpcDialogue: mockGenerateNpcDialogue,
  }));
  ```

  测试策略：**函数单元测试**（function-unit approach），直接 import hook 工厂函数并调用（参考 src/ui/hooks/use-streaming-text.test.ts 的 createStreamingText 模式）。不使用 renderHook — bun test 环境无 React DOM，renderHook 需额外配置。直接测试 startDialogue 调用了哪些参数、messagesRef 状态，以及 resetMessages() 行为。

  运行 `bun test src/ui/hooks/use-npc-dialogue.test.ts` 确认 RED（新测试 FAIL 或文件存在但 mock 正确设置）。
  </action>
  <verify>
    <automated>bun test src/ui/hooks/use-npc-dialogue.test.ts 2>/dev/null | grep -E "FAIL|pass|fail" | head -10</automated>
  </verify>
  <done>测试文件存在，运行有 FAIL（RED 状态）</done>
  <acceptance_criteria>
    - ls src/ui/hooks/use-npc-dialogue.test.ts 存在
    - grep "messagesRef\|accumulate\|resetMessages" src/ui/hooks/use-npc-dialogue.test.ts 有输出
    - bun test src/ui/hooks/use-npc-dialogue.test.ts 有 FAIL（RED 确认）
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: [GREEN] 实现 messagesRef 积累 + resetMessages + narrative-creation-screen 接线</name>
  <files>
    src/ui/hooks/use-npc-dialogue.ts,
    src/ui/screens/narrative-creation-screen.tsx
  </files>
  <read_first>
    - src/ui/hooks/use-npc-dialogue.ts （修改前完整读取）
    - src/ui/screens/narrative-creation-screen.tsx （修改前完整读取——找到 loadRetryKey useEffect 和所有 reset() 调用）
    - src/ui/hooks/use-npc-dialogue.test.ts （确认 Task 1 写的期望）
  </read_first>
  <behavior>
    【use-npc-dialogue.ts】
    1. NpcDialogueContext.conversationHistory 类型迁移（若 Wave 2 未处理）：{speaker,text}[] → {role:'user'|'assistant', content:string}[]
    2. 新增 messagesRef = useRef<Array<{role:'user'|'assistant'; content:string}>>([])（与 contextRef 同位置声明）
    3. reset() 保持现有行为（streaming.reset + setMetadata(null)），不清 messagesRef
    4. 新增 resetMessages = useCallback(() => { messagesRef.current = []; }, [])
    5. startDialogue 内：streaming.start 调用 streamNpcDialogue 时，options.conversationHistory 使用 messagesRef.current（当长度 > 0 时）而非 context.conversationHistory
    6. 流式完成 useEffect（lines 68-108）：在 setMetadata 调用后，追加本轮记录到 messagesRef：
       messagesRef.current = [...messagesRef.current, {role:'user',content:ctx.playerAction}, {role:'assistant',content:fullText}]
    7. 同样在 fallback generateNpcDialogue 调用中传入 conversationHistory: messagesRef.current（若非空）
    8. UseNpcDialogueReturn 类型追加 resetMessages: () => void
    9. return 对象追加 resetMessages

    【narrative-creation-screen.tsx】
    1. UseNpcDialogueReturn 中 resetMessages 现在可用，在 loadRetryKey useEffect 内、setPhase({type:'round_streaming', round:1}) 前调用 npcDialogue.resetMessages()
    2. 不修改任何 reset() 调用位置（reset 仍只清流式状态）
    3. npcDialogue.startDialogue call site 无需修改（hook 内部使用 messagesRef.current）
  </behavior>
  <action>
  修改 use-npc-dialogue.ts：
  1. 在 const contextRef = useRef 一行下方追加: const messagesRef = useRef<Array<{role:'user'|'assistant'; content:string}>>([]);
  2. 在 const originalReset = streaming.reset 块处：reset() 内部不加清 messagesRef 的逻辑。
  3. 在 reset() 的 return 下方追加 resetMessages 函数。
  4. 修改 startDialogue 的 streaming.start 调用，将 conversationHistory 改为: messagesRef.current.length > 0 ? messagesRef.current : context.conversationHistory
  5. 在流式完成 useEffect 中，setMetadata 调用后追加 messagesRef 的 append 逻辑（spread，不 push）。
  6. 在 fallback generateNpcDialogue 调用的 options 中同样传入 conversationHistory: messagesRef.current.length > 0 ? messagesRef.current : ctx.conversationHistory
  7. UseNpcDialogueReturn 和 return 对象均加入 resetMessages。

  修改 narrative-creation-screen.tsx：
  在 loadRetryKey useEffect 中的 setPhase({ type: 'round_streaming', round: 1 }) 语句前插入 npcDialogue.resetMessages();

  完成后运行测试。
  </action>
  <verify>
    <automated>bun test src/ui/hooks/use-npc-dialogue.test.ts 2>/dev/null | tail -5</automated>
  </verify>
  <done>use-npc-dialogue.test.ts 全部 PASS；全量套件无新失败</done>
  <acceptance_criteria>
    - grep "messagesRef" src/ui/hooks/use-npc-dialogue.ts 输出至少 4 行
    - grep "resetMessages" src/ui/hooks/use-npc-dialogue.ts 有输出
    - grep "resetMessages" src/ui/screens/narrative-creation-screen.tsx 有输出（loadRetryKey useEffect 内）
    - grep "reset()" src/ui/hooks/use-npc-dialogue.ts | grep -v "messagesRef" 存在（reset 未清 messagesRef）
    - bun test src/ui/hooks/use-npc-dialogue.test.ts 输出全 pass，0 fail
    - bun test 2>/dev/null | tail -3 显示 0 fail（全量回归）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| messagesRef.current → streamNpcDialogue | 内部积累的对话历史，由 hook 构建，非直接用户输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Tampering | messagesRef.current 内容 | accept | content 来自 ctx.playerAction（固定选项 label）和 fullText（NPC AI 输出），均由内部构建，无外部注入路径 |
| T-18-03-02 | Denial of Service | 无限积累 messages | accept | D-09 明确不限历史长度；守卫对话固定 4 轮，上限可预测；正常 NPC 对话 session 会在 resetMessages() 边界重置 |
</threat_model>

<verification>
```bash
# DIAL-03 完成验证
grep -n "messagesRef" src/ui/hooks/use-npc-dialogue.ts
grep -n "resetMessages" src/ui/hooks/use-npc-dialogue.ts src/ui/screens/narrative-creation-screen.tsx

# 全量测试
bun test 2>/dev/null | tail -3
# 期望：0 fail

# Phase 18 完整成功标准验证
bun test src/ai/utils/ai-caller.test.ts src/engine/dialogue-manager.test.ts src/ui/hooks/use-npc-dialogue.test.ts 2>/dev/null
```
</verification>

<success_criteria>
- useNpcDialogue hook 持有 messagesRef，流式完成后自动追加 {role,content}[] 记录
- reset() 不清 messagesRef；resetMessages() 清空
- UseNpcDialogueReturn 导出 resetMessages
- narrative-creation-screen.tsx 在守卫对话开始前调用 resetMessages()
- use-npc-dialogue.test.ts 全部通过（含 3 轮积累测试）
- 全量套件 0 fail
</success_criteria>

<output>
完成后在 .planning/phases/18-multi-turn-dialogue/ 创建 18-P03-SUMMARY.md
</output>
