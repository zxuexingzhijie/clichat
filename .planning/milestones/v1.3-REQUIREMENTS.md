# Requirements Archive — v1.3 Story Mainline & Narrative System

**Archived:** 2026-04-30
**Status:** ✅ All v1.3 requirements complete

---

## v1.3 Requirements (All Completed)

### NARRATIVE — Story State & Act System

- [x] **D-01**: Create `NarrativeStateSchema` with `currentAct` (act1/act2/act3), `atmosphereTags` (string[]), `worldFlags` (Record<string, boolean>), `playerKnowledgeLevel` (0–5)
- [x] **D-02**: Create `createNarrativeStore` factory and wire into `createGameContext`
- [x] **D-03**: Create `narrative-transitions.yaml` mapping quest stage IDs to act/atmosphere/worldFlag transitions
- [x] **D-04**: `createNarrativeStateWatcher` subscribes to `quest_stage_advanced` events and applies transitions
- [x] **D-05**: SaveDataV5 extends V4 with `narrativeState`; migration chain V4→V5 complete; restore chain handles V5→V4→V3 fallback

### PROMPT INJECTION — AI Narrative Enhancement

- [x] **D-06**: `buildNarrativeSystemPrompt` accepts optional `NarrativePromptContext` (act + atmosphereTags + recentNarration) and injects act-appropriate tone guidance
- [x] **D-07**: `buildNpcSystemPrompt` accepts `trustLevel` (0–10) and gates knowledge disclosure at 3 tiers
- [x] **D-08**: `NpcKnowledgeProfile` type with `always_knows`, `hidden_knowledge`, `trust_gates` array
- [x] **D-09**: `narrativeStore` wired into `SceneManagerOptions` and `DialogueManagerOptions`; helpers extract context at all call sites

### QUEST — Conditional Stage Advancement

- [x] **D-10**: `ConditionalNextStageSchema` with `conditions` array (flag + condition_value) and `next_stage_id`
- [x] **D-11**: `QuestStageSchema.conditional_next_stages` optional field
- [x] **D-12**: `resolveNextStage` function evaluates flags at advancement time; first-match-wins; falls back to `nextStageId`
- [x] **D-13**: `quest_main_01` expanded to 8 stages: 5 linear (rumor → disappearances → forest → mayor_secret → allies_decision) + 3 endings (justice/harmony/shadow)
- [x] **D-14**: Side quest descriptions include mainline binding context

### NPC — Knowledge Profile & Trust

- [x] **D-15**: `NpcTrustGateSchema` and `NpcKnowledgeProfileSchema` added to Zod schemas and exported
- [x] **D-16**: `NpcSchema.knowledge_profile` optional field
- [x] **D-17**: `knowledge_profile` populated in `npcs.yaml` for 7 story NPCs (王德, 老陈, 林婆婆, 月华, 瞎子孙, 阿鬼, 陈铁柱)
- [x] **D-18**: `dialogue-manager` reads `personalTrust` from relation store, maps to 0–10 `trustLevel`, passes to `buildNpcSystemPrompt`
- [x] **D-19**: `tryLockRouteFlag` sets `justice_score_locked`/`shadow_score_locked`/`pragmatism_score_locked` on `endDialogue` at `stage_allies_decision`
- [x] **D-20**: `quest` field in `createDialogueManager` stores is optional (backward-compatible with existing test fixtures)

### LOCATION — Description Overrides

- [x] **D-21**: `LocationSchema.description_overrides: z.record(z.string(), z.string()).optional()`
- [x] **D-22**: `description_overrides` authored for `loc_tavern`, `loc_north_gate`, `loc_main_street` (3 flags each)
- [x] **D-23**: `selectLocationDescription` pure function with `OVERRIDE_PRIORITY` list; deterministic — no LLM call when flag matches
- [x] **D-24**: `handleLook` (no-target) and `loadScene` use `selectLocationDescription` via `narrativeStore.getState().worldFlags`

---

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| D-01 to D-05 | Phase 16-P01 | ✅ Complete |
| D-06 to D-09 | Phase 16-P02 | ✅ Complete |
| D-10 to D-14 | Phase 16-P03 | ✅ Complete |
| D-15 to D-20 | Phase 16-P04 | ✅ Complete |
| D-21 to D-24 | Phase 16-P05 | ✅ Complete |

---

## Requirement Outcomes

| Requirement | Original Intent | Actual Outcome | Notes |
|-------------|----------------|----------------|-------|
| D-18 trust mapping | personalTrust=0 → trustLevel < 5 | personalTrust=0 → trustLevel=5 | Formula `(0+100)/20=5` is semantically correct (neutral); test adjusted |
| D-09 narrativeContext in npc-actor | Pass as 6th param | Accepted as 6th param; trustLevel wired as 7th | generateNpcDialogue signature grew to 7 params |

---

*Archived from .planning/REQUIREMENTS.md after v1.3 milestone completion*
